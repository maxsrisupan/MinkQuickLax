<Project Sdk="Microsoft.NET.Sdk">

  <!-- Builds manual/<lang>/*.md into one self-contained HTML file per language (PLAN.md M9). -->

  <PropertyGroup>
    <OutputType>Exe</OutputType>
  </PropertyGroup>

</Project>
