# MinkQuickLax — ข้อกำหนด (Spec)

> **เอกสารหลัก:** ถ้าขัดกับเอกสารอื่น ให้ยึดไฟล์นี้
> ฉบับ 1.1 · 2026-09-15
> **เอกสารที่เกี่ยวข้อง**
> - [PLAN.md](PLAN.md): วิธีทำ ลำดับงาน และความคืบหน้า
> - [DESIGN-DISCUSSION.md](../DESIGN-DISCUSSION.md): บันทึกการคุยออกแบบ ใช้ดูเหตุผลย้อนหลัง
> - Mockup: https://claude.ai/code/artifact/0a7e4095-33f8-4eb2-8301-298bd6f00bd2 (v0.3 มีสไตล์ Glass, HUD, Dot Matrix)

---

## 1. ภาพรวม

MinkQuickLax เป็น quick launcher สำหรับ Windows
- link แต่ละตัวเป็น **ไอคอนลอยบนสุดของจอ** วางไว้ตรงไหนก็ได้ กดแล้วเปิดทันที
- รวม link หลายตัวเป็น **กลุ่ม** ได้ ใช้ไอคอนเดี่ยวกับกลุ่มปนกันได้
- ไม่แย่ง focus จาก app ที่ใช้อยู่ ไม่โผล่ใน taskbar หรือ Alt+Tab
- **ฟรีและ open source** (MIT) · มีภาษาไทยและอังกฤษ

### ไม่ทำ (non-goals)
- ไม่ทำเป็น Windows Service และตัว app ไม่รันเป็น admin
- ไม่มีกลุ่มซ้อนในกลุ่ม
- ไม่มีสวิตช์โหมด "เดี่ยว / กลุ่ม / ทั้งคู่"
- ไม่เก็บหรือส่งข้อมูลการใช้งานออกไป (ไม่มี telemetry)
- ไม่รองรับ macOS หรือ Linux

---

## 2. การตัดสินใจหลัก

| เรื่อง | ตัดสินใจ |
|---|---|
| รูปแบบบนจอ | ไอคอนเดี่ยวกับกลุ่ม มีกี่อันก็ได้ ใช้ปนกันได้ |
| ข้อมูล | แยก **link** (ข้อมูล) ออกจาก **ของที่วางบนจอ** link เดียววางได้หลายที่ |
| สไตล์ | **Glass** เป็นค่าเริ่มต้น · **HUD** และ **Dot Matrix** ทำในเฟส 1 ด้วย · ผู้ใช้เลือกในหน้าตั้งค่า เปลี่ยนแล้วมีผลทันทีกับทุกชิ้น |
| ภาษา | ไทยและอังกฤษ ค่าเริ่มต้นตามภาษาของ Windows |
| การแจก | ฟรี · open source สัญญาอนุญาต MIT · repo สาธารณะบน GitHub |
| เครื่องมือ | C# .NET 10 + WPF (รายละเอียดใน PLAN.md) |
| ตัวติดตั้ง | Velopack · ติดตั้งแบบรายผู้ใช้ ไม่ต้องใช้สิทธิ์ admin · อัปเดตตัวเองได้ |
| Windows ที่รองรับ | Windows 11 22H2 ขึ้นไปใช้ได้ครบ · Windows 10 22H2 ใช้ได้ แต่ไม่มีเอฟเฟกต์เบลอ |
| เซ็นโปรแกรม | ช่วงแรกยังไม่เซ็น ใส่วิธีกด Run anyway ไว้ในคู่มือ |
| คีย์ลัด | `Win+Alt+Q` (เฟส 2) |
| ที่เก็บค่าตั้ง | JSON ไฟล์เดียว พร้อมสำรองอัตโนมัติ |

---

## 3. คำศัพท์ (ใช้ชื่อเดียวกันในโค้ด)

| คำในเอกสาร | ชื่อในโค้ด | ความหมาย |
|---|---|---|
| link | `Link` | สิ่งที่เปิดได้ เช่น app, ไฟล์, โฟลเดอร์, URL มีชื่อ icon และเป้าหมาย |
| ของบนจอ | `Placement` | ไอคอนเดี่ยวหรือกลุ่มที่วางอยู่บนจอ ชี้ไปหา link หรือกลุ่ม |
| ไอคอนเดี่ยว | `LinkPlacement` | ของบนจอที่ชี้ไปหา link 1 ตัว |
| กลุ่ม | `Group` | ชุดของ link แสดงเป็นโฟลเดอร์หรือแถบ วางบนจอได้ 1 ที่ |
| โหมดใช้งาน | `UseMode` | ตำแหน่งล็อก กดเพื่อเปิด |
| โหมดแก้ไข | `EditMode` | ลากจัดวางได้ ไอคอนสั่น มีแถบเครื่องมือด้านบนจอ |
| หน้าค้นหาด่วน | `QuickSearch` | หน้าต่างที่เด้งขึ้นเมื่อกดคีย์ลัด (เฟส 2) |
| สไตล์ | `Style` | ชุดหน้าตา: Glass, HUD, Dot Matrix |

---

## 4. ฟีเจอร์

ป้าย **[F1] [F2] [F3]** บอกว่าอยู่เฟสไหน

### 4.1 Link

**ประเภท**

| ประเภท | `kind` | ตัวอย่าง | เฟส |
|---|---|---|---|
| โปรแกรม | `app` | `.exe`, `.lnk` | F1 |
| app จาก Start Menu/Store | `shellApp` | ชื่อจาก `shell:AppsFolder` เช่น `Microsoft.WindowsCalculator_8wekyb3d8bbwe!App` | F1 |
| ไฟล์ | `file` | `.pdf`, `.xlsx` | F1 |
| โฟลเดอร์ | `folder` | `D:\งาน` | F1 |
| เว็บ | `url` | `https://…` | F1 |
| คำสั่ง/script | `command` | `.bat`, `.ps1`, คำสั่งพร้อม argument | F2 |
| หน้าตั้งค่า Windows | `msSettings` | `ms-settings:display` | F2 |

**ข้อมูลของ link:** ชื่อ, icon, เป้าหมาย, argument, working directory, เปิดในฐานะผู้ดูแลระบบ (ราย link), browser ที่ใช้เปิด (เฉพาะ `url`)

**ช่องทางเพิ่ม link**
- [F1] สแกนหา app ในเครื่อง: รายชื่อจาก `shell:AppsFolder` (เหมือน All apps) และ shortcut บน Desktop · มีช่องค้นหา · กรองได้: ทั้งหมด/โปรแกรม/Store/Desktop · ติ๊กเลือกหลายตัวแล้วเพิ่มทีเดียว · แก้ชื่อได้ก่อนเพิ่ม
- [F1] เพิ่มเอง: ปุ่ม "ไฟล์…" และ "โฟลเดอร์…" ในหน้าสแกน (ขึ้นในรายการพร้อมติ๊กไว้ แล้วกด "เพิ่มลงจอ")
- [F1] **หน้าต่าง "เพิ่มเว็บ"** เพิ่ม link เว็บทีละตัวในขั้นเดียว
  - **ทางเปิด:** เมนูที่ tray "เพิ่มเว็บ…" · ปุ่ม "เว็บ…" ในหน้าสแกน · ปุ่ม "เพิ่มเว็บ…" ในหน้าตั้งค่าหมวด Link · เปิดจากหน้าสแกนหรือหน้าตั้งค่าแล้วอยู่กลางหน้าต่างนั้น เปิดจาก tray อยู่กลางจอ
  - **ช่อง:** ที่อยู่เว็บ (รับ focus ตอนเปิด มีตัวอย่าง "เช่น youtube.com" ข้างหัวช่อง) · ชื่อ พร้อม icon ตัวอย่าง · เปิดด้วย (browser)
  - **ที่อยู่:** พิมพ์แค่ชื่อเว็บได้ เช่น `youtube.com` จะเติม `https://` ให้ (`localhost` และเลข IP เติม `http://`) · รับ `http`, `https`, `mailto:`, `ms-settings:` และ scheme อื่นที่เป็นที่อยู่ · ไม่อ่าน clipboard เอง
  - **ชื่อ:** เติมจากชื่อเว็บ (ตัด `www.`) และเปลี่ยนตามที่อยู่จนกว่าผู้ใช้จะพิมพ์ชื่อเอง · เว้นว่างไว้ใช้ชื่อเว็บ
  - **icon ตัวอย่าง:** หยุดพิมพ์ครู่หนึ่ง (0.6 วินาที) แล้วดึง favicon จากเว็บนั้นโดยตรง ใช้ต่อเป็น icon ของ link ที่เพิ่ม · ระหว่างนั้นหรือถ้าไม่มีใช้ icon ตัวอักษรของชื่อ · ยังไม่มีชื่อแสดงช่องว่าง
  - **"เพิ่มลงจอ" หรือ Enter:** กดได้เมื่อช่องที่อยู่มีข้อความ · เพิ่ม link แล้ววางบนจอเหมือนหน้าสแกน แล้วปิดหน้าต่าง · ที่อยู่ใช้ไม่ได้ให้แสดงข้อความใต้ช่อง ไม่เพิ่มและไม่ปิด
  - **มีอยู่แล้ว:** ถ้ามี link ที่อยู่เดียวกันอยู่แล้ว แสดงข้อความเตือนใต้ช่อง แต่ยังเพิ่มได้ (เช่น เว็บเดียวกันแต่เปิดคนละ browser)
  - "ยกเลิก", ✕ หรือ Esc ปิดโดยไม่เพิ่ม · เปิดได้ทีละหน้าต่าง · หน้าตาและพื้นแบบเดียวกับหน้าสแกน กว้าง 440px
- [F2] ลากไฟล์, shortcut หรือ URL จาก browser มาวางบนที่ว่างหรือบนกลุ่ม

**เปิดเว็บด้วย browser ที่เลือก [F1]**
- link เว็บเลือกได้ว่าเปิดด้วย browser ไหน: **browser หลักของ Windows** (ค่าเริ่มต้น) หรือ browser ที่ติดตั้งในเครื่อง เช่น Google Chrome, Microsoft Edge, Firefox
- รายชื่อ browser อ่านจากที่ Windows ใช้แสดงในหน้าเลือก browser (`StartMenuInternet` ทั้งของเครื่องและของผู้ใช้) พร้อม icon ของ browser
- เลือกได้ในหน้าต่างเพิ่มเว็บ และในหน้าแก้ link
- กดไอคอนแล้วเปิด URL นั้นใน browser ที่เลือกทันที
- ถ้า browser ที่เลือกถูกถอนออกไปแล้ว ให้เปิดด้วย browser หลักแทน และหน้าแก้ link แสดงว่าไม่พบ browser นั้น
- icon ของ link ยังเป็น favicon ของเว็บ

**icon**
1. [F1] ดึงจาก app ความละเอียด 256px
2. [F1] เลือกไฟล์ PNG หรือ ICO เอง · [F2] SVG
3. [F1] favicon สำหรับ URL ดึงจากเว็บนั้นโดยตรง ไม่ผ่านบริการภายนอก
4. [F1] ตัวอักษรบนพื้นสี เมื่อไม่มี icon (ดูหัวข้อ 5.3)

**วางหลายที่และการลบ**
- link เดียวเป็นไอคอนเดี่ยวได้หลายตัว และอยู่ได้หลายกลุ่ม (กลุ่มละไม่เกิน 1 ครั้ง)
- แก้ชื่อหรือ icon ครั้งเดียว เปลี่ยนทุกที่
- **เอาออกจากจอ:** ลบแค่ของบนจอชิ้นนั้น link ยังอยู่ในรายการ
- **ลบ link:** หายจากทุกที่ ต้องถามยืนยัน และบอกว่าวางอยู่กี่ที่

### 4.2 ไอคอนเดี่ยว [F1]

**โหมดใช้งาน**
- ตำแหน่งล็อกไว้ **คลิกเดียว** เพื่อเปิด (เปลี่ยนเป็นดับเบิลคลิกได้)
- ชี้ค้าง 350ms แล้วชื่อขึ้น หรือตั้งให้แสดงชื่อใต้ไอคอนตลอด
- ตอนไม่ได้ใช้ ความทึบ 70% · เมาส์เข้าใกล้ภายใน 130px จะชัดขึ้นจนเต็ม
- ขยายตามเมาส์ได้สูงสุด 1.16 เท่า (ปิดได้)
- กดแล้วไอคอนเด้งหนึ่งครั้ง
- **Ctrl+ลาก:** ย้ายไอคอนได้ทันทีโดยไม่ต้องเข้าโหมดแก้ไข (ปิดได้)

**เมนูคลิกขวา**
1. เปิด
2. เปิดในฐานะผู้ดูแลระบบ (เฉพาะ `app`, `command`)
3. เปิดตำแหน่งที่เก็บ (เฉพาะ `app`, `file`, `folder`)
4. ─
5. วางเพิ่มบนจอ
6. เพิ่มเข้ากลุ่ม… (เลือกกลุ่มที่มีอยู่ หรือสร้างใหม่)
7. แก้ไข… (เปิดหน้าตั้งค่าที่ link นี้)
8. จัดวางไอคอน (เข้าโหมดแก้ไข)
9. ─
10. เอาออกจากจอ
11. ลบ link… (สีแดง)

### 4.3 กลุ่ม

| แบบ | ตอนปกติ | ตอนใช้ | เฟส |
|---|---|---|---|
| **โฟลเดอร์** (ค่าเริ่มต้น) | ไอคอนเดียว ขนาดเท่าไอคอนเดี่ยว ข้างในเป็นไอคอนย่อ 2×2 ของ 4 ตัวแรก วาดตามสไตล์ (หัวข้อ 5) | คลิกแล้วกางเป็นแผง | F1 |
| **แถบ** | ไอคอนเรียงเป็นแถวแนวนอนหรือแนวตั้ง มีชื่อกลุ่ม | กดได้เลย ไม่ต้องกาง | F2 |

- **เปิดโฟลเดอร์:** คลิก (ค่าเริ่มต้น) · [F2] ตั้งให้ชี้แล้วกางได้ รอ 200ms ก่อนกาง
- **ทิศที่กาง:** เลือกให้เองตามพื้นที่ว่างบนจอ เช่นโฟลเดอร์ชิดขอบขวาให้กางไปทางซ้าย
- **แผงหุบเมื่อ:** กด link, กด Esc, คลิกที่อื่น
- **ในแผง:** แสดงไอคอนเป็นตาราง มีชื่อกลุ่มด้านบน ชื่อ link ใต้ไอคอน
- **ค่าตั้งของกลุ่ม:** ชื่อ, แบบ (โฟลเดอร์/แถบ), icon (ไอคอนย่อ 2×2 หรือเลือกเอง), จำนวนคอลัมน์ (ค่าเริ่มต้น 4), แสดงชื่อ link
- **เมนูคลิกขวาที่กลุ่ม:** เปิดทั้งหมด [F3], แก้ไขกลุ่ม…, จัดวางไอคอน, เอาออกจากจอ, ลบกลุ่ม… (link ข้างในไม่ถูกลบ)
- **กลุ่มว่าง:** ยังอยู่บนจอ แสดงเป็นโฟลเดอร์เปล่า

### 4.4 จัดวางและลากวาง

**เข้าโหมดแก้ไข:** เมนูคลิกขวาที่ไอคอน/กลุ่ม หรือเมนูที่ tray → "จัดวางไอคอน"

**โหมดแก้ไข [F1]**
- ไอคอนทุกตัวบอกว่ากำลังแก้ไข ชื่อไอคอนซ่อน · Glass: สั่นเบา ๆ · HUD: วงเล็บล็อกเป้าสีเหลืองอำพันกะพริบ · Dot Matrix: วงจุดหมุนรอบไอคอน
- วางแล้วชิดกริด 24px หรือชิดแนวไอคอนอื่นเมื่อห่างไม่เกิน 8px มีเส้นประช่วยจัดแนวยาวตลอดจอ
- ระหว่างลากมีป้ายบอกพิกัดข้างไอคอน เช่น `X 0456  Y 0312` (จุดกลางไอคอน นับจากมุมซ้ายบนของพื้นที่ทำงาน) · ไม่ขยายตามเมาส์
- วางทับกันไม่ได้ ถ้าปล่อยทับ ไอคอนจะขยับไปช่องว่างที่ใกล้ที่สุด
- **ลากค้างบนไอคอนเดี่ยวอีกตัว 0.6 วินาที:** ไอคอนเป้าหมายขยายขึ้นเป็นสัญญาณ ปล่อยแล้วสร้างกลุ่มใหม่จากสองตัวนั้น
- **แป้นพิมพ์:** ลูกศรขยับทีละ 24px (Shift+ลูกศร ทีละ 1px) · Delete = เอาออกจากจอ · Ctrl+Z = ย้อนกลับ · Ctrl+Y = ทำซ้ำ
- **แถบเครื่องมือด้านบนจอ:** เพิ่ม · จัดเรียงใหม่ · ยกเลิก (คืนทุกอย่างก่อนเข้าโหมด) · เสร็จ
- [F2] เลือกหลายตัว: ลากเป็นกรอบ หรือ Ctrl+คลิก แล้วลากไปพร้อมกัน

**สิ่งที่ลากได้ในโหมดแก้ไข**

| ลากอะไร | ไปวางที่ไหน | ผลที่ได้ | เฟส |
|---|---|---|---|
| ไอคอนเดี่ยว | ที่ว่างบนจอ | ย้ายตำแหน่ง | F1 |
| ไอคอนเดี่ยว | บนกลุ่ม | ย้ายเข้ากลุ่ม (ไอคอนเดี่ยวนั้นหายไป) | F1 |
| ไอคอนเดี่ยว | ค้างบนไอคอนเดี่ยวอีกตัว 0.6 วินาที | สร้างกลุ่มใหม่ | F1 |
| link ในแผงกลุ่ม | ออกนอกแผง | ย้ายออกจากกลุ่มมาเป็นไอคอนเดี่ยว | F1 |
| link ในแผงกลุ่ม | ตำแหน่งอื่นในแผง | จัดลำดับใหม่ | F1 |
| กลุ่มทั้งกลุ่ม | ที่ว่างบนจอ | ย้ายทั้งกลุ่ม | F1 |
| link จากรายการในหน้าตั้งค่า | บนจอ หรือบนกลุ่ม | วางเพิ่ม | F2 |
| ไฟล์/shortcut/URL จากนอก app | ที่ว่าง หรือบนกลุ่ม | สร้าง link ใหม่ | F2 |

**กติกาตำแหน่ง**
- **ค่าเริ่มต้นวางได้แค่ในพื้นที่ทำงาน** ไม่ทับ taskbar · เปิดให้วางทับได้ในค่าขั้นสูง
- **เก็บตำแหน่ง** เป็นสัดส่วน 0–1 ของพื้นที่ทำงานของจอนั้น วัดที่จุดกลางไอคอน และจำว่าอยู่จอไหน
- **เปลี่ยนความละเอียดหรือ DPI:** คำนวณตำแหน่งใหม่จากสัดส่วน ถ้าหลุดขอบให้ดึงกลับเข้าจอ
- **จอที่เคยวางไว้หายไป:** แสดงบนจอหลักชั่วคราว แต่ไม่แก้ตำแหน่งที่เก็บไว้ เสียบจอกลับแล้วกลับไปที่เดิม ถ้าผู้ใช้ย้ายเองระหว่างนั้นจึงบันทึกตำแหน่งใหม่
- **จัดเรียงใหม่:** เรียงไอคอนและกลุ่มทั้งหมดเป็นตารางชิดมุมขวาบนของจอหลัก เรียงตามลำดับที่เพิ่ม

### 4.5 ซ่อน/แสดง

- [F1] **คลิก icon ที่ tray:** ซ่อน/แสดงทั้งชุด (ปิดได้ ถ้าปิด การคลิกจะเปิดเมนูแทน)
- [F2] **คีย์ลัดซ่อน/แสดงทั้งชุด:** ค่าเริ่มต้นไม่ได้ตั้ง
- [F2] **ซ่อนเองตอนเปิด app เต็มจอ หรือตอน present:** ค่าเริ่มต้นเปิด · ออกจากเต็มจอแล้วแสดงกลับ
- ซ่อน/แสดงพร้อมกันทุกชิ้น ไล่ห่างกันชิ้นละ 14ms
- เปิด app ใหม่จะแสดงเสมอ ไม่จำว่าเคยซ่อนไว้ ผู้ใช้จะได้ไม่คิดว่าไอคอนหาย
- ตอนซ่อนอยู่ tooltip ของ icon ที่ tray ต้องบอกว่า "ซ่อนอยู่"

### 4.6 คีย์ลัดและหน้าค้นหาด่วน [F2]

- **ค่าเริ่มต้น `Win+Alt+Q`** · กดแล้วหน้าค้นหาด่วนเด้งขึ้นตรงตำแหน่งเมาส์
- กดเลข 1–9 เพื่อเปิด หรือพิมพ์ค้นหาได้ทันที · Enter เปิดตัวแรก · Esc ปิด
- ใช้ได้แม้ตอนไอคอนซ่อนอยู่หรือเปิด app เต็มจอ
- **หน้าตั้งคีย์ลัด:** กดคีย์ที่อยากใช้เพื่อบันทึก ถ้าตรงกับตารางด้านล่างให้ขึ้นเตือน · ปิดคีย์ลัดได้
- **ถ้าลงทะเบียนไม่ผ่าน:** แจ้งเตือนที่ tray แล้วเปิดหน้าตั้งคีย์ลัด
- **ข้อจำกัด:** `RegisterHotKey` ตรวจเจอเฉพาะ app ที่จองคีย์ด้วยวิธีเดียวกัน ส่วน app ที่ใช้ keyboard hook ตรวจไม่เจอ

**คีย์ที่ต้องเตือน**

| คีย์ | ชนกับ |
|---|---|
| `Ctrl+Alt+Space` | Claude Desktop |
| `Alt+Space` | PowerToys Run, Copilot, เมนูหน้าต่างของ Windows |
| `Win+Alt+Space` | PowerToys Command Palette |
| `Ctrl+Space` | PowerToys Peek, สลับ IME, autocomplete ใน editor |
| `Ctrl+Shift+…` | CapsLock ที่แมปด้วย AutoHotkey, คีย์ลัดใน app ทั่วไป |
| `Win+Space`, `Alt+Shift+…`, `` ` `` | สลับภาษา |
| `Ctrl+Alt+ตัวอักษร` | AltGr บนคีย์บอร์ดยุโรป |
| `Win+Alt+` B, D, G, H, K, M, R, T, U, P | Windows และ PowerToys |

### 4.7 icon ที่ tray [F1]

- **คลิกซ้าย:** ซ่อน/แสดง (หัวข้อ 4.5)
- **คลิกขวา:** เพิ่มจากเครื่องนี้… · เพิ่มเว็บ… · จัดวางไอคอน · จัดเรียงใหม่ · ─ · ซ่อน/แสดง · ─ · หน้าตั้งค่า · คู่มือ · ตรวจหาอัปเดต · ─ · ออก
- Explorer รีสตาร์ทแล้วต้องเพิ่ม icon กลับเอง
- **icon ของ app** (tray, หัวหน้าต่างตั้งค่าและคู่มือ, หน้าเกี่ยวกับ, exe, ตัวติดตั้ง): วงแหวนแสงรูปตัว Q ไล่สี `#2FD9BE` → ขาวอมฟ้า → `#FFC15A` เว้นช่องที่มุมล่างขวาให้หาง `#FF7250` พุ่งออกไปจบที่จุดสว่าง บนแผ่นมุมโค้งสีหมึก `#0F2331` → `#04080C` มีขอบแสงและแสงเรือง · ขนาด 32px ขึ้นไปมีวงประจาง ๆ รอบวงแหวน · ขนาด 16–24px แผ่นเต็มกรอบ วงและหางหนาขึ้น ไม่มีวงประ · ต้นฉบับคือ `build/app-icon.svg` และ `build/app-icon-small.svg`

### 4.8 หน้าตั้งค่า

**หลักการ**
- **หน้าตา:** เป็นหน้าต่างปกติ (โผล่ใน taskbar และ Alt+Tab ได้) แต่วาดเป็นแผ่นตามสไตล์ที่เลือก (Glass, HUD, Dot Matrix) เหมือนแผงปรับหน้าตาใน mockup
  - เมนูหมวดอยู่ด้านซ้าย เนื้อหาเป็นกลุ่มค่าตั้งที่มีหัวข้อเล็ก
  - ตัวควบคุมตามหัวข้อ 5.10: ปุ่มแบ่งช่อง, slider ที่แสดงค่า, สวิตช์, ช่องเลือกสีกระจก
  - เลือกสไตล์ด้วยปุ่ม 3 ช่องที่มีภาพตัวอย่าง เปลี่ยนแล้วหน้าตั้งค่าเปลี่ยนตามทันที
- **ปรับแล้วมีผลทันที** ไม่มีปุ่มบันทึก · แต่ละหมวดมีปุ่ม "คืนค่าเริ่มต้น"
- [F1] **ช่องค้นหาค่าตั้ง:** ค้นได้ทั้งไทยและอังกฤษ
- ค่าที่ไม่ค่อยได้ปรับพับไว้ใต้ "ขั้นสูง"
- [F2] **ค่าตั้งแต่ละข้อมีปุ่ม ⓘ** เปิดคู่มือตรงหัวข้อนั้น · กด F1 เพื่อเปิดคู่มือ

**หมวด**

| หมวด | ค่าตั้ง (ค่าเริ่มต้น) | เฟส |
|---|---|---|
| **Link** | รายการ link ทั้งหมด ค้นหาได้ กรองตามประเภท และกรอง "ยังไม่ได้วางไว้ที่ไหน" · เพิ่มเว็บ, สแกนหา app, แก้ไข, ลบ · ชื่อ, icon, เป้าหมาย, argument, working directory, run as admin, browser ที่ใช้เปิด (link เว็บ) · ดูว่าวางอยู่ที่ไหนบ้าง | F1 |
| **กลุ่ม** | สร้าง/ลบ · แบบโฟลเดอร์ (F1) หรือแถบ (F2) · จำนวนคอลัมน์ (4) · ลำดับ link · แสดงชื่อ link (เปิด) | F1 |
| **หน้าตา** | สไตล์: Glass/HUD/Dot Matrix (Glass) · ธีม: ตาม Windows/สว่าง/มืด (ตาม Windows) · ขนาดไอคอน 32–64px (48) · ความทึบตอนไม่ได้ใช้ 30–100% (70%) · ระยะที่เริ่มชัดขึ้น (130px) · ขยายตามเมาส์ (เปิด) · แสดงชื่อใต้ไอคอน (ปิด) · ลดภาพเคลื่อนไหว (ตาม Windows) · ค่าของสไตล์ Glass (หัวข้อ 5.6 แสดงเมื่อเลือก Glass) | F1 |
| **ซ่อน/แสดง** | คลิก icon ที่ tray เพื่อซ่อน/แสดง (เปิด) · ซ่อนตอนเปิด app เต็มจอหรือ present (เปิด, F2) | F1 |
| **การใช้งาน** | เปิดด้วยคลิกเดียว/ดับเบิลคลิก (คลิกเดียว) · เวลาชี้ค้างก่อนชื่อขึ้น (350ms) · เปิดกลุ่มด้วยคลิก/ชี้ (คลิก, F2) · สลับไปหน้าต่างเดิมถ้า app เปิดอยู่ (ปิด, F3) | F1 |
| **ตำแหน่ง** | ขนาดกริด (24px) · การชิด: กริดและแนว/กริด/ไม่ชิด (กริดและแนว) · Ctrl+ลากเพื่อย้าย (เปิด) · ขั้นสูง: วางทับ taskbar ได้ (ปิด) · จัดเรียงใหม่ · คืนตำแหน่งทั้งหมด | F1 |
| **คีย์ลัด** | คีย์เรียกหน้าค้นหาด่วน (`Win+Alt+Q`) · คีย์ซ่อน/แสดง (ไม่ได้ตั้ง) · ปิดคีย์ลัดทั้งหมด | F2 |
| **ทั่วไป** | เปิดพร้อม Windows (เปิด) · ภาษา: ตาม Windows/ไทย/English · ตรวจหาอัปเดตอัตโนมัติ (เปิด) · ช่องทางอัปเดต: ปกติ/ทดลอง (ปกติ) | F1 |
| **ข้อมูล** | สำรองตอนนี้ · กู้คืนจากไฟล์สำรอง · เปิดโฟลเดอร์ค่าตั้ง · คืนค่าทั้งหมด · ลบข้อมูลทั้งหมดและออก · [F3] นำเข้า/ส่งออก | F1 |
| **เกี่ยวกับ** | เวอร์ชัน · เปิดคู่มือ · หน้า GitHub · สัญญาอนุญาตของ app และส่วนประกอบที่นำมาใช้ · คัดลอกข้อมูลสำหรับแจ้งปัญหา (เวอร์ชัน app/Windows, จอ, DPI, log ล่าสุด 200 บรรทัด) | F1 |

### 4.9 คู่มือ (HTML)

- **ใช้ได้โดยไม่ต้องมีเน็ต:** ติดมากับตัวติดตั้ง ฟอนต์ รูป CSS และ JS รวมอยู่ในไฟล์ ไม่โหลดจาก CDN
- **เปิดในหน้าต่างของ app (WebView2):** ไปตรงหัวข้อได้ ธีมตรงกับ app · มีปุ่ม "เปิดใน browser" และ "พิมพ์"
  - ถ้าเครื่องไม่มี WebView2 Runtime ให้เปิดใน browser แทน (ไปตรงหัวข้อไม่ได้)
- **สไตล์เดียวกับ app** รองรับธีมสว่างและมืด
- **สารบัญด้านซ้าย:** ไฮไลต์หัวข้อที่กำลังอ่าน · หน้าต่างแคบให้พับเป็นเมนู
- **ค้นหา**
  - กด `/` หรือ `Ctrl+K` · ผลขึ้นทันทีระหว่างพิมพ์ พร้อมข้อความรอบคำที่เจอ · ↑↓ เลือก · Enter ไปที่หัวข้อแล้วไฮไลต์คำ
  - หาแบบข้อความย่อย (substring) ไม่สนตัวพิมพ์เล็ก/ใหญ่ · ไม่ใช้ library ที่ตัดคำด้วยช่องว่าง เพราะใช้กับภาษาไทยไม่ได้
  - ใช้ `Intl.Segmenter('th')` ไว้ไฮไลต์และจัดอันดับผล
  - แต่ละหัวข้อมีคำค้นเพิ่มทั้งไทยและอังกฤษ เช่น "hotkey" เจอหัวข้อ "คีย์ลัด"
- **สองภาษา:** เปิดตามภาษาของ app มีปุ่มสลับภาษา · ทั้งสองภาษาใช้ id หัวข้อเดียวกัน (ภาษาอังกฤษ เช่น `settings-idle-opacity`) · สลับภาษาแล้วยังอยู่หัวข้อเดิม
  - [F1] เนื้อหาภาษาไทย · [F2] เนื้อหาภาษาอังกฤษ ระหว่างนี้หัวข้อที่ยังไม่แปลแสดงภาษาไทยพร้อมป้ายบอก
- **ทางเปิด:** เมนูที่ tray, หน้าเกี่ยวกับ, F1 ในหน้าตั้งค่า, ปุ่ม ⓘ [F2]

**เนื้อหา**
1. เริ่มต้นใช้งาน: ติดตั้ง, หน้าเตือน SmartScreen (วิธีกด More info → Run anyway), เปิดครั้งแรก
2. เพิ่ม link: สแกนหา app, เพิ่มเอง, ลากมาวาง
3. ไอคอนเดี่ยวและกลุ่ม
4. จัดวาง: โหมดแก้ไข, Ctrl+ลาก, ใช้หลายจอ
5. ซ่อน/แสดง, คีย์ลัด, หน้าค้นหาด่วน
6. ค่าตั้งทุกข้อ (1 ข้อ = 1 หัวข้อย่อย มี id ตรงกับปุ่ม ⓘ)
7. แก้ปัญหา: ไอคอนหาย, คีย์ลัดไม่ทำงาน, ไอคอนโดนบัง, ไม่เปิดพร้อม Windows, กู้ค่าตั้ง
8. สำรองข้อมูล, ย้ายเครื่อง, ถอนการติดตั้ง, ลบข้อมูลที่เหลือ
9. ความเป็นส่วนตัว: app ติดต่ออินเทอร์เน็ตแค่ตอนตรวจอัปเดตและดึง favicon
10. คำถามที่พบบ่อย

### 4.10 เปิดพร้อม Windows [F1]

- เปิดตอน **เข้าสู่ระบบ** ไม่ใช่ตอนบูตเครื่อง
- ลงทะเบียนที่ `HKCU\Software\Microsoft\Windows\CurrentVersion\Run` ชี้ไปที่ path ของ exe ที่ไม่เปลี่ยนเมื่ออัปเดต และส่ง argument `--startup`
- **ต้องตรงกับ Task Manager:** อ่านสถานะจาก `HKCU\...\Explorer\StartupApproved\Run` ด้วย ถ้าผู้ใช้ปิดใน Task Manager หน้าตั้งค่าต้องแสดงว่าปิด และห้ามเปิดกลับเอง
  - สถานะจริงอ่านจาก registry ทุกครั้ง ไม่เก็บลงไฟล์ค่าตั้ง
- **`--startup`:** แสดงแค่ไอคอนกับ tray ไม่เปิดหน้าต่างใด ๆ
- **ค่าเริ่มต้น:** เปิด ลงทะเบียนตอนเปิด app ครั้งแรก

### 4.11 ติดตั้ง อัปเดต ถอนการติดตั้ง [F1]

- **ตัวติดตั้ง:** `MinkQuickLax-win-Setup.exe` ติดตั้งแบบรายผู้ใช้ ไม่มีหน้าต่าง UAC · รวม .NET ไว้ในตัว
- **Shortcut:** Start Menu เท่านั้น
- **ติดตั้งเสร็จ:** เปิด app ทันที
- **อัปเดต**
  - ตรวจตอนเปิด app และทุก 24 ชั่วโมง
  - เจอเวอร์ชันใหม่ให้ดาวน์โหลดเงียบ ๆ แล้วแจ้งที่ tray: "มีเวอร์ชันใหม่ จะติดตั้งตอนปิด app" พร้อมปุ่ม "รีสตาร์ทตอนนี้"
  - ช่องทาง: ปกติ (stable) และทดลอง (beta)
  - ปิดการตรวจอัตโนมัติได้ ยังกด "ตรวจหาอัปเดต" เองได้
- **ถอนการติดตั้ง**
  - ลบรายการเปิดพร้อม Windows
  - **ไม่ลบค่าตั้งใน `%AppData%\MinkQuickLax\`** เพราะ Velopack แสดงหน้าถามระหว่างถอนไม่ได้ ลงใหม่แล้วได้ค่าเดิมกลับมา
  - ถ้าต้องการลบจริงใช้ปุ่ม "ลบข้อมูลทั้งหมดและออก" ในหมวดข้อมูล หรือทำตามคู่มือ

### 4.12 เปิดครั้งแรก

- **[F1]** ถ้ายังไม่มี link เลย ให้เปิดหน้าสแกนหา app ทันที โดยเพิ่ม link ที่เลือกเป็นไอคอนเดี่ยวเรียงชิดมุมขวาบนของจอหลัก
- **[F2] หน้าต้อนรับ** กดข้ามได้ทุกขั้น
  1. เลือกภาษาและสไตล์
  2. สแกนหา app แล้วติ๊กตัวที่ใช้บ่อย
  3. จัดวางตั้งต้น: ไอคอนเดี่ยวทั้งหมด หรือรวมเป็นกลุ่มเดียว
  4. เปิดพร้อม Windows และตั้งคีย์ลัด
  5. สอนสั้น ๆ: Ctrl+ลาก, คลิกขวา, icon ที่ tray

### 4.13 ภาษา [F1]

- ข้อความทั้งหมดใน app มีภาษาไทยและอังกฤษครบ
- **ค่าเริ่มต้น:** ถ้า Windows เป็นภาษาไทยใช้ไทย นอกนั้นใช้อังกฤษ
- **เปลี่ยนภาษา:** มีผลทันทีโดยไม่ต้องเปิด app ใหม่
- **ชื่อ link:** เป็นข้อมูลของผู้ใช้ ไม่แปล

### 4.14 ฟีเจอร์เฟส 3
- **เปิดหลาย app ทีเดียว (Launch set)**
- **จุดบอกว่า app เปิดอยู่** และกดแล้วสลับไปหน้าต่างเดิมแทนการเปิดซ้ำ (เลือกได้)
- **นำเข้า/ส่งออกค่าตั้ง:** ถ้าไฟล์ที่นำเข้ามี link ประเภท `command` หรือ run as admin ต้องแสดงรายการให้ผู้ใช้ยืนยันก่อน
- **รองรับหลายจอเต็มรูปแบบ:** ย้ายทั้งชุดไปจออื่น และจัดวางแยกชุดตามการต่อจอ
- **ติดขอบจอ (Edge dock)**
- **รุ่นสำหรับ Windows บน ARM (arm64)**

---

## 5. สไตล์ [F1]

ค่าทั้งหมดมาจาก mockup v0.3 แล้วปรับให้ Windows ทำได้จริง · ทุกสไตล์ใช้ขนาดหน้าต่าง กติกา และจังหวะการเคลื่อนไหวเดียวกัน (5.4) ต่างกันที่การวาด · หัวข้อ 5.1–5.7 คือ **Glass** · 5.8 คือ **HUD** · 5.9 คือ **Dot Matrix** · 5.10 คือตัวควบคุมในหน้าตั้งค่า

### 5.1 ค่าสี (Glass)

| token | ธีมสว่าง | ธีมมืด | ใช้กับ |
|---|---|---|---|
| `GlassRgb` | `255 255 255` | `18 30 36` | สีพื้นกระจก |
| `GlassInk` | `#0D1B1A` | `#F1FAF8` | ตัวอักษรบนกระจก |
| `GlassInk2` | `#0D1B1A` ทึบ 64% | `#F1FAF8` ทึบ 66% | ตัวอักษรรอง |
| `GlassEdge` | ขาว 55% | ขาว 20% | ขอบ 1px |
| `GlassHighlight` | ขาว 80% | ขาว 32% | เส้นไฮไลต์ด้านบน 1px |
| `GlassRim` | ขาว 100% | ขาว 85% | ขอบแสงตรงมุมที่สว่างที่สุด (5.2) |
| `GlassSheen` | ขาว 60% | ขาว 24% | แสงเรืองที่มุม (5.2) |
| `GlassHairline` | `#0D1B1A` ทึบ 16% | ดำ 35% | เส้นขอบนอกสุด 1px |
| `GlassShadow` | y 24, blur 60, `#082828` ทึบ 45% | y 28, blur 70, ดำ 70% | เงาของแผ่นกระจก |
| `TileShadow` | y 6, blur 14, `#081E1E` ทึบ 45% | y 8, blur 18, ดำ 70% | เงาไอคอน |
| `Accent` | `#00789A` | `#5EE6FF` | ปุ่มหลัก, สวิตช์, focus |
| `AccentInk` | `#FFFFFF` | `#02141A` | ตัวอักษรบนสี Accent |
| `Danger` | `#D23F28` | `#FF5A43` | คำสั่งลบ |
| `Guide` | `#FF7250` | `#FF7250` | เส้นช่วยจัดแนว, จุดบอกโหมดแก้ไข |

- **สีกระจกให้เลือก:** ตามธีม (`GlassRgb`) · Lagoon `47 217 190` · Ember `255 114 80` · Ink `10 22 28`
- **ธีม:** สว่าง / มืด / ตาม Windows (ค่าเริ่มต้น) · ตาม Windows ใช้ค่า "app mode" และเปลี่ยนตามทันที · HUD ใช้สีมืดเสมอ

### 5.2 สูตรแผ่นกระจก (Glass)

- **พื้น:** ไล่สีเฉียง 135° จากสีกระจกทึบ `a+0.12` → `a` (ที่ 55%) → `a+0.05` โดย `a` = ความทึบของสีกระจก (ค่าเริ่มต้น 0.18)
- **แสงบนกระจก (ตามภาพอ้างอิงที่ผู้ใช้ให้มา):** แสงมาจากมุมบนซ้าย
  - **เส้นนอก:** 1px `GlassHairline` ชิดขอบ ให้แผ่นคมบนพื้นสีเดียวกัน
  - **ขอบแสง:** 1px ถัดเข้ามา ไล่สีเฉียงตามกรอบของแผ่น: มุมบนซ้าย `GlassRim` → ทึบ 70% ที่ 16% → `GlassEdge` ช่วง 42–64% (รอบมุมขวาบนและซ้ายล่าง) → `GlassRim` ทึบ 45% ที่ 86% → มุมล่างขวา `GlassRim` ทึบ 75%
  - **เส้นบน:** 1px `GlassHighlight` จางหายไปทางขวาจนหมดที่ 70% ของความกว้าง
  - **แสงเรือง:** วงกลมไล่จาก `GlassSheen` ที่มุมบนซ้าย (ทึบ 50% ที่ 35% ของรัศมี แล้วจางหมด) รัศมี = ครึ่งด้านสั้น + ¼ ด้านยาว อยู่ในช่วง 40–260px · มุมล่างขวามีวงเล็กกว่า รัศมี 45% ความแรง 60% · วาดใต้เนื้อหา ตัดตามรูปแผ่น
  - **เส้นคั่น:** จางที่ปลายทั้งสองข้าง (20% แรกและสุดท้าย)
  - **แถวที่ชี้, ช่องที่เลือก:** พื้นขาวไล่จากบน 32% ลงล่าง 18% · ขอบไล่จากบน `GlassRim` ทึบ 70% ลงล่าง `GlassEdge` ทึบ 40% (ช่องที่เลือก 80% → 50%)
  - **ปุ่มสี Accent:** แสงขาว 30% ที่ขอบบนจางหมดที่ 60% ของความสูง + ขอบ 1px ไล่จากบนขาว 55% ลงล่างใส
- **เงา:** `GlassShadow`
- **เบลอข้างหลัง:** ใช้ Acrylic ของ Windows (ดูหัวข้อ 5.5) · ในเว็บ mockup คือ blur 28px, saturate 170%

| ชิ้นส่วน | ความทึบ `a` | มุมโค้ง | เบลอ | ขนาดและระยะ |
|---|---|---|---|---|
| ชื่อไอคอน (tooltip) | 0.82 | 8px | มี | padding 4×10 · ตัวอักษร 12.5px · ห่างไอคอน 10px |
| เมนูคลิกขวา | 0.58 | 8px | มี | กว้างอย่างน้อย 210px · padding 6 · รายการ padding 7×10 มุม 8px · ตัวอักษร 13.5px · ชี้แล้วเป็นแถวกระจก (ดูแสงบนกระจกด้านบน) |
| แถบเครื่องมือโหมดแก้ไข | 0.18 | 8px | มี | กลางด้านบนจอ ห่างขอบ 14px · ปุ่มมุม 8px · ปุ่ม "เสร็จ" ใช้สี Accent · มีจุด `Guide` 7px หน้าข้อความ |
| แผงกลุ่มที่กาง | 0.18 | 8px | มี | padding 10 · ช่องละ ขนาดไอคอน + 24px |
| หน้าสแกนหา app | 0.42 | 8px | มี | กว้าง 560px |
| หน้าตั้งค่า | 0.42 | 8px | มี | กว้าง 1000px · เมนูหมวดกว้าง 220px |
| แผ่นโฟลเดอร์ (ตอนไม่กาง) | 0.55 | 29% ของขนาด | **ไม่มี** | ไอคอนย่อ 2×2 แต่ละตัวกว้าง 36% ของขนาดโฟลเดอร์ ห่างกัน 8% · มีแสงบนกระจกเหมือนแผ่นอื่น (แสงอยู่ใต้ไอคอนย่อ) |
| ป้ายพิกัดตอนลาก | 0.78 | 8px | ไม่มี | ตัวอักษร JetBrains Mono 11px |

### 5.3 ไอคอน (Glass)

- **ไอคอนเดี่ยว:** วาดภาพ icon ของ app ตรง ๆ พร้อม `TileShadow` ไม่มีแผ่นรอง ไม่เบลอ
- **icon แบบตัวอักษร**
  - สี่เหลี่ยมมุมโค้ง 29% · ไล่สีเฉียง 150° ตามคู่สีที่เลือกจาก hash ของชื่อ
  - แสงขาว 30% ที่ขอบบนจางหมดที่กลางไอคอน · ขอบใน 2% ของขนาด ไล่แสงเฉียงแบบขอบแสงของกระจก (5.2) สว่างสุดขาว 55% หม่นสุดขาว 11% · เส้นบนขาว 50% จางไปทางขวา
  - ตัวอักษรตัวแรกของชื่อ (นับเป็น grapheme ให้สระและวรรณยุกต์ไทยไม่หลุด) ฟอนต์ Chakra Petch 700 ขนาด 40% ของไอคอน สีขาว
  - **คู่สี:** `#2FD9BE→#1E8F9E` · `#2F6FD9→#1B3F8F` · `#FFC15A→#E08A1E` · `#FF7250→#C73E2A` · `#FF5C8A→#B8325F` · `#8E6CF0→#5A3FB8` · `#7ED67A→#3E9A4E` · `#7F95A3→#4B5F69`
- **ชื่อใต้ไอคอน:** 11px สีขาว มีเงาตัวอักษร · กว้างเท่าไอคอน + 20px ยาวเกินให้ตัดด้วย …
- **หน้าต่างของไอคอน:** ขนาดไอคอน + ขอบรอบละ 12px (ไอคอน 48px → หน้าต่าง 72×72) เผื่อไว้สำหรับเงาและการขยาย

### 5.4 การเคลื่อนไหว

| เหตุการณ์ | ค่า |
|---|---|
| เส้นโค้ง | spring `cubic-bezier(.34,1.56,.64,1)` · out `cubic-bezier(.2,.7,.3,1)` |
| เมาส์เข้าใกล้ | ความทึบและขนาดตามระยะเมาส์ ปรับตามภายใน 250ms · ขยายสุด 1.16 เท่า |
| ชื่อไอคอนขึ้น | รอ 350ms · จางเข้า 150ms และเลื่อนขึ้น 6px แบบ spring 250ms |
| กดเปิด | ไอคอนเด้ง 750ms + วงขาวกระจายออก 700ms |
| เมนูคลิกขวา | ย่อจาก 95% และจางเข้า 160ms |
| แผงกลุ่มกาง | ขยายจากตำแหน่งโฟลเดอร์ 420ms (out) |
| โหมดแก้ไข | สั่นเอียงสลับไปมา รอบละ 260ms แต่ละตัวเริ่มไม่พร้อมกัน · แถบเครื่องมือเลื่อนลงแบบ spring 450ms |
| กำลังลาก | ขยาย 1.12 เท่า เงาเข้มขึ้น |
| จัดเรียงใหม่ | ทุกชิ้นเลื่อนพร้อมกัน spring 550ms |
| เพิ่มไอคอนใหม่ | เด้งเข้า spring 600ms ห่างกันตัวละ 90ms |
| ซ่อน/แสดง | จางและย่อเหลือ 80% ภายใน 250–300ms ห่างกันชิ้นละ 14ms |
| เปลี่ยนสไตล์ | ไล่เปลี่ยนทีละชิ้น ห่างกัน 45ms |
| ลดภาพเคลื่อนไหว | ถ้า Windows ปิด Animation effects หรือผู้ใช้เปิด "ลดภาพเคลื่อนไหว" ให้ข้ามทุกอย่างในตารางนี้ และการเคลื่อนไหวของ HUD และ Dot Matrix (5.8, 5.9) |

### 5.5 ทำบน Windows จริง

- **ไอคอนเดี่ยว, แผ่นโฟลเดอร์, ป้ายพิกัด:** หน้าต่างโปร่งใส (layered) วาดกระจกเองโดยไม่เบลอ
- **ชื่อไอคอน, เมนู, แถบเครื่องมือ, แผงกลุ่ม, หน้าสแกน**
  - Windows 11 22H2 ขึ้นไป: ใช้ acrylic ของ Windows (accent policy ผ่าน `SetWindowCompositionAttribute` เพราะ `DWMWA_SYSTEMBACKDROP_TYPE` ไม่เบลอบนหน้าต่างที่ไม่รับ focus) มุมโค้ง 8px ของระบบ **ไม่มีเส้นขอบของระบบ** (เส้นเทาของ Windows ทับขอบแสง) แล้ววางพื้นและแสงตามหัวข้อ 5.2 ทับ · หน้าสแกนเป็นหน้าต่างปกติ ใช้ Acrylic ของระบบได้
  - Windows 10 และ Windows 11 ก่อน 22H2: Windows ไม่เบลอ ใช้กระจกฝ้า (ด้านล่าง)
- **กระจกฝ้า (Windows ไม่เบลอให้):** เมื่อผู้ใช้ปิด Transparency effects ของ Windows หรือ Windows เก่ากว่า 11 22H2 แต่ยังเปิดเบลอในหน้าตั้งค่า
  - แผ่นกระจกทั้งหมด (รวมหน้าสแกน หน้าตั้งค่า คู่มือ) วาดภาพพื้นหลัง desktop ส่วนที่อยู่หลังหน้าต่างไว้ใต้สีกระจก แล้วใช้ความทึบ `a` ตามตาราง 5.2 เหมือนตอนเบลอ
  - อ่านภาพพื้นหลังรายจอผ่าน `IDesktopWallpaper` วางตามแบบที่ Windows ใช้ (เต็มจอ, พอดีจอ, ยืด, กึ่งกลาง, เรียงต่อ, ต่อข้ามจอ) บนสีพื้นหลังของ desktop · slideshow และ Spotlight ใช้ภาพปัจจุบันจาก registry
  - ย่อภาพของแต่ละจอเหลือกว้าง 192px เบลอรัศมี 10px (ประมาณ 1/20 ของความกว้างจอ) และเพิ่มความสด 150% ทำครั้งเดียวต่อจอ ทำใหม่เมื่อภาพหรือจอเปลี่ยน
  - เห็นเฉพาะภาพพื้นหลัง ไม่เห็นหน้าต่างอื่นที่อยู่ข้างหลัง (ไม่ถ่ายภาพหน้าจอมาเบลอเอง เพราะต้องทำใหม่ทุกครั้งที่หน้าต่างข้างหลังเปลี่ยน กินเครื่อง และเป็นการอ่านเนื้อหาของ app อื่น)
- **ใช้พื้นทึบ `a = 0.92` เมื่อ:** เปิดโหมดประหยัดแบต · ปิดการเบลอในหน้าตั้งค่า
- **HUD และ Dot Matrix:** แผ่นพื้นไม่เบลอ ใช้ความทึบตามตาราง · ส่วนที่อยู่นอกรูปทรง (มุมที่ตัดของ HUD, มุมโค้งของ Dot Matrix) ต้องโปร่งใส · ไม่มีขอบและมุมโค้งของระบบ
- **High contrast:** ใช้สีของระบบทั้งหมด ไม่มีกระจก ทุกสไตล์

### 5.6 ค่าตั้งของสไตล์ Glass

| ค่าตั้ง | ค่าเริ่มต้น | ช่วง |
|---|---|---|
| สีกระจก | ตามธีม | ตามธีม, Lagoon, Ember, Ink |
| ความทึบของสี | 18% | 0–70% (ใช้กับแผ่นที่ค่า `a` = 0.18 ในหัวข้อ 5.2) |
| เบลอข้างหลัง | เปิด | เปิด/ปิด (ปรับความแรงไม่ได้ เพราะ Windows กำหนดเอง) |
| แสงสะท้อนวิ่งตามเมาส์ | เปิด | เปิด/ปิด [F2] |

### 5.7 ฟอนต์

| ใช้กับ | ฟอนต์ |
|---|---|
| ชื่อไอคอน, เมนู, แถบเครื่องมือ, แผงกลุ่ม, หน้าสแกน, คู่มือ (เนื้อหา) | IBM Plex Sans Thai → Leelawadee UI → Segoe UI |
| ตัวอักษรใน icon, หัวข้อในคู่มือ | Chakra Petch |
| ป้ายพิกัด, โค้ดในคู่มือ | JetBrains Mono → Cascadia Mono |
| หน้าตั้งค่า | ฟอนต์ของสไตล์ที่เลือก เหมือนหน้าสแกน |
| HUD | Chakra Petch (หัวข้อ, ชื่อ) · JetBrains Mono (ลำดับ, ข้อมูล, ป้าย) · ภาษาไทยใช้ IBM Plex Sans Thai |
| Dot Matrix | Doto 800 (ตัวเลข) · IBM Plex Sans Thai · JetBrains Mono (หัวเมนู, ข้อมูล) |

ฟอนต์ทั้งหมดเป็น OFL ฝังไปกับ app ได้ ต้องใส่ไฟล์สัญญาอนุญาตไว้ในหน้าเกี่ยวกับ

### 5.8 สไตล์ HUD [F1]

`t` คือความใกล้ของเมาส์ 0–1 ตามหัวข้อ 4.2 (0 = อยู่ไกลเกิน 130px, 1 = อยู่บนไอคอน)

| ชิ้นส่วน | ค่า |
|---|---|
| สี | cyan `#6FF3FF` · amber `#FFB23E` · แดง `#FF4D6D` · พื้นแผ่น `#030A10` ทึบ 86% · ตัวอักษร `#DDF7FF` · ตัวอักษรรอง `#B4DEEB` ทึบ 64% · ขอบ cyan 24% · Accent `#6FF3FF` บนตัวอักษร `#021016` |
| แผ่นพื้น (ชื่อไอคอน, เมนู, กล่องแจ้งเตือน, แถบเครื่องมือ, แผงกลุ่ม, หน้าสแกน, หน้าตั้งค่า) | ตัดมุมบนซ้ายและล่างขวา 10px · ขอบใน 1px cyan 26% · เส้นมุม cyan 2px ยาว 12px ที่มุมขวาบนและซ้ายล่าง · เส้นสแกนแนวนอน cyan 3% ทุก 3px · ไม่เบลอ · ตัวควบคุมข้างในเป็นมุมเหลี่ยม |
| ไอคอน | แผ่นรองเท่าขนาดไอคอน ตัดมุมบนซ้ายและล่างขวา 22% · พื้น `#030A10` ทึบ 80% ไล่ cyan 16% จากมุมบนซ้ายถึงกลาง · เส้นทแยงบาง cyan 70% ที่ 11% และ 89% · ขอบใน cyan ทึบ 28% + 50%×t · เรืองแสง cyan blur 3 + 9×t px ทึบ 12% + 40%×t · ภาพ icon อยู่ข้างในห่างขอบ 21% มุมโค้ง 24% |
| ตอนพักและเมาส์เข้าใกล้ | ภาพ icon เป็นโฮโลแกรม: ขาวดำย้อมสีฟ้า มีเส้นสแกน cyan 30% ทุก 3px ทับ · ยิ่งเมาส์ใกล้ สีจริงยิ่งกลับมาและเส้นสแกนจางลง |
| ชี้ที่ไอคอน | วงเล็บล็อกเป้า 4 มุม ขายาว 8px หนา 1.5px หดจากห่างขอบ 10px เป็น 5px แบบ spring 300ms · เส้นแสงกวาดจากบนลงล่าง 700ms |
| ชื่อไอคอน | ป้ายด้านข้างไอคอน (ขวา หรือซ้ายถ้าที่ไม่พอ) มีเส้นเชื่อม cyan 70% ยาว 14px · ซ้ายสุดเป็นลำดับของไอคอน `01` (JetBrains Mono 10px cyan) · ชื่อ Chakra Petch 600 13.5px · บรรทัดข้อมูลตัวพิมพ์ใหญ่ JetBrains Mono 10px เช่น `APP · CHROME.EXE` พิมพ์ทีละตัวใน 400ms หลังชื่อขึ้น 450ms · มุมบนซ้ายและล่างขวามีเส้น cyan 7px |
| ชื่อใต้ไอคอน | JetBrains Mono 9.5px ตัวพิมพ์ใหญ่ สี `#CFF6FF` บนพื้น `#030A10` ทึบ 76% |
| เมนูคลิกขวา | กว้างอย่างน้อย 232px · หัวเมนู JetBrains Mono 10.5px ตัวพิมพ์ใหญ่ cyan · รายการมุมเหลี่ยม ชี้แล้วพื้น cyan 10% ขอบ cyan 30% และมี ▸ หน้ารายการ · คำสั่งลบสีแดง · เส้นคั่น cyan 18% |
| แถบเครื่องมือโหมดแก้ไข | ข้อความ JetBrains Mono 11px สี amber มีจุดสี่เหลี่ยม amber เรืองแสงกะพริบ · ปุ่มเหลี่ยม ชี้แล้วพื้น cyan 12% · ปุ่ม "เสร็จ" ตัดมุม 7px สี Accent |
| โหมดแก้ไข | ไม่สั่น · วงเล็บล็อกเป้าสี amber ค้างไว้และกะพริบทุก 1.1 วินาที |
| ลาก | เส้นช่วยจัดแนว cyan เส้นประ 4px เว้น 4px · ป้ายพิกัดมุมเหลี่ยม ตัวอักษร cyan บนพื้น `#030A10` 88% |
| กดเปิด | กรอบ cyan 1.5px รูปแผ่นรองกระจายออก + เส้นแสงกวาด |
| หน้าสแกนและหน้าตั้งค่า | ตัวเลือกที่เลือกอยู่พื้น cyan 16% ขอบ cyan 50% · แถบความคืบหน้าไล่ cyan ถึง amber · ปุ่มหลักตัดมุม 8px |
| โฟลเดอร์ | แผ่นรองแบบไอคอน ข้างในไอคอนย่อ 2×2 เป็นโฮโลแกรม |
| เปลี่ยนมาใช้สไตล์นี้ | ทุกไอคอนกวาดแสงและล็อกเป้า ไล่ทีละตัวห่างกัน 45ms |

### 5.9 สไตล์ Dot Matrix [F1]

| ชิ้นส่วน | ค่า |
|---|---|
| สี (ตามธีม) | สว่าง: พื้น `#F4F4F1` · ตัวอักษร `#111111` · รอง `#676763` · เส้น ดำ 13% · แดง `#E0261C` ── มืด: พื้น `#141414` · ตัวอักษร `#F1F1ED` · รอง `#9B9B96` · เส้น ขาว 15% · แดง `#FF3B30` ── Accent คือสีตัวอักษรบนสีพื้น |
| แผ่นพื้น | ทึบ ไม่เบลอ · มุมโค้ง 18px (ชื่อไอคอน 14px) · ขอบ 1px สีเส้น · เงา y 18 blur 40 ดำ 60% |
| ไอคอน | วงกลม · ภาพ icon ตัดเป็นเม็ดจุดบนตาราง 4px รัศมีจุด 1.1px + 1.95px×t (t = 1 จุดติดกันเต็ม) · ตอนพักขาวดำ contrast +50% สว่าง +25% สีจริงกลับมาตาม t · ไม่มีเงา |
| ชี้ที่ไอคอน | วงจุด 2px สีขาวมีเงาบาง ๆ ห่างขอบไอคอน 6px ขยายจาก 85% แบบ spring 350ms และหมุนรอบละ 6 วินาที |
| ชื่อไอคอน | ป้ายด้านข้างไอคอน มุมโค้ง 14px · ลำดับ `01` Doto 800 26px · ชื่อ IBM Plex Sans Thai 600 13px · บรรทัดข้อมูล JetBrains Mono 10px ตัวพิมพ์ใหญ่สีรอง |
| ชื่อใต้ไอคอน | ป้ายแคปซูลพื้นทึบ IBM Plex Sans Thai 10.5px |
| เมนูคลิกขวา | หัวเมนู JetBrains Mono 10.5px ตัวพิมพ์ใหญ่ มีจุดแดง 6px นำหน้า · ชี้แล้วพื้นสีตัวอักษร 7% · เส้นคั่นเป็นจุด 2px |
| แถบเครื่องมือโหมดแก้ไข | ข้อความ JetBrains Mono 11px · จุดแดงกะพริบ · ปุ่ม "เสร็จ" แคปซูล |
| โหมดแก้ไข | ไม่สั่น · วงจุดหมุนค้างไว้ |
| ลาก | เส้นช่วยจัดแนวเป็นจุดสีแดงรัศมี 1.2px ทุก 6px · ป้ายพิกัดแคปซูล Doto 800 13px |
| กดเปิด | วงจุดกระจายออก |
| หน้าสแกนและหน้าตั้งค่า | ตัวเลือกที่เลือกอยู่ใช้พื้นสีตัวอักษรกับตัวอักษรสีพื้น · ปุ่มหลักแคปซูล · เส้นคั่นเป็นจุด · icon ในรายการเป็นวงกลม |
| โฟลเดอร์ | วงกลมพื้นทึบ ข้างในไอคอนย่อ 2×2 เป็นเม็ดจุด |
| เปลี่ยนมาใช้สไตล์นี้ | จุดของทุกไอคอนเต็มวงแล้วหดกลับเป็นเม็ดจุดใน 900ms ไล่ทีละตัวห่างกัน 40ms |

### 5.10 ตัวควบคุมในหน้าตั้งค่า (ตามแผงปรับหน้าตาใน mockup)

| ตัวควบคุม | Glass | HUD | Dot Matrix |
|---|---|---|---|
| หัวกลุ่มค่าตั้ง | 12px 600 สีรอง | JetBrains Mono ตัวพิมพ์ใหญ่ cyan | JetBrains Mono ตัวพิมพ์ใหญ่ มีจุดแดงนำหน้า |
| ปุ่มแบ่งช่อง | รางพื้นสีขอบ padding 3 มุม 5 · ช่องที่เลือกพื้นทึบ ขอบใน Accent 1px | มุมเหลี่ยม ช่องที่เลือกพื้น cyan 16% ขอบ cyan 50% | แคปซูล ช่องที่เลือกกลับสี |
| ตัวเลือกสไตล์ | ปุ่มแบ่งช่อง 3 ช่อง มีภาพตัวอย่าง 20px เหนือชื่อ (HUD: แผ่นตัดมุมมีจุด · Dot Matrix: วงเม็ดจุดมีจุดแดง · Glass: ลูกแก้วสี) | เหมือนกัน | เหมือนกัน |
| slider | ชื่อซ้าย ค่าขวา (JetBrains Mono 11.5px สีรอง) · รางและปุ่มสี Accent | รางเหลี่ยม | รางเป็นจุด |
| สวิตช์ | ราง 36×20 มุม 4 · ปุ่ม 14×14 มุม 2 · เปิดแล้วรางสี Accent ปุ่มเลื่อน 16px แบบ spring 300ms | มุม 0 | แคปซูล ปุ่มกลม |
| สีกระจก (เฉพาะ Glass) | วงกลม 30px ไล่สี 135° ชื่อใต้วง 11.5px · ที่เลือกมีวงนอก Accent 2px | – | – |
| ช่องพิมพ์, รายการ, ปุ่ม | ตามหน้าสแกน | ตามหน้าสแกน | ตามหน้าสแกน |

---

## 6. ข้อกำหนดที่ไม่ใช่ฟีเจอร์

| เรื่อง | ข้อกำหนด |
|---|---|
| **focus** | กดไอคอน กลุ่ม หรือเมนู แล้ว focus ต้องยังอยู่ที่ app เดิม · ยกเว้นโหมดแก้ไข หน้าสแกน หน้าเพิ่มเว็บ หน้าตั้งค่า และหน้าค้นหาด่วน ที่ต้องรับการพิมพ์ |
| **taskbar / Alt+Tab** | ไอคอน กลุ่ม เมนู และแถบเครื่องมือ ต้องไม่โผล่ · หน้าตั้งค่ากับคู่มือโผล่ได้ |
| **บนสุด** | ไอคอนต้องกลับขึ้นบนสุดเมื่อมีหน้าต่างอื่นที่ตั้งบนสุดไว้เหมือนกันมาบัง |
| **ประสิทธิภาพ** (เป้า ต้องวัดจริง) | ไอคอน 30 ชิ้น: RAM ไม่เกิน 120MB · CPU ตอนเมาส์ไม่ขยับเกือบ 0% · ตอนขยับเมาส์ผ่านไอคอนไม่เกิน 3% · เปิด app จนไอคอนขึ้นไม่เกิน 2 วินาที |
| **DPI และหลายจอ** | Per-Monitor DPI v2 · ไอคอนคมทุก scale · ลากข้ามจอที่ scale ต่างกันได้ |
| **ความทนทาน** | เปิดได้ทีละตัว · บันทึกไฟล์ด้วยการเขียนไฟล์ชั่วคราวแล้วค่อยแทนที่ · สำรอง 10 ชุดล่าสุด · ไฟล์เสียให้กู้จากชุดสำรองล่าสุดที่อ่านได้ แล้วแจ้งผู้ใช้ · crash แล้วต้องมี log |
| **log** | `%LocalAppData%\MinkQuickLax\logs` เก็บ 7 วัน · ไม่บันทึกข้อมูลส่วนตัวนอกจาก path ของ link |
| **ความปลอดภัย** | ตัว app ไม่รันเป็น admin · run as admin ขอสิทธิ์เป็นราย link ผ่าน UAC ปกติ · ไม่เรียกคำสั่งผ่าน shell string ที่ต่อเอง |
| **ความเป็นส่วนตัว** | ติดต่ออินเทอร์เน็ตแค่ตรวจอัปเดต (GitHub) และดึง favicon จากเว็บที่ผู้ใช้ลงทะเบียน · ไม่มี telemetry |
| **การเข้าถึง** | หน้าตั้งค่าใช้งานด้วยแป้นพิมพ์ได้ครบ และมีชื่อสำหรับ screen reader · ทำตามค่า Animation effects และ High contrast ของ Windows |
| **ภาษา** | ไม่มีข้อความเขียนตรงในโค้ด · UI ต้องไม่ตัดสระบน/ล่างของภาษาไทย · ปุ่มและป้ายต้องยืดตามความยาวข้อความภาษาอังกฤษ |
| **OS** | Windows 10 22H2 (build 19045) ขึ้นไป · x64 · ทดสอบหลักบน Windows 11 |

---

## 7. กรณีที่ต้องรองรับ

| กรณี | สิ่งที่ต้องทำ |
|---|---|
| เปิดเกม วิดีโอเต็มจอ หรือ present งาน | [F2] ซ่อนทั้งชุด ออกแล้วแสดงกลับ |
| เมาส์แค่ผ่านแต่กลุ่มกางเอง | กางด้วยการคลิกเป็นค่าเริ่มต้น · แบบชี้ต้องรอ 200ms |
| กดไอคอนตอนกำลังพิมพ์งาน | ไม่แย่ง focus (หัวข้อ 6) |
| หลายจอ, scale ไม่เท่ากัน, เปลี่ยนความละเอียด | หัวข้อ 4.4 กติกาตำแหน่ง |
| ถอดจอที่มีไอคอน | แสดงบนจอหลักชั่วคราว เสียบกลับแล้วคืนที่เดิม |
| ตอนเข้า Windows หรือตื่นจาก sleep จอยังไม่พร้อม | จัดตำแหน่งใหม่ทุกครั้งที่จอหรือ DPI เปลี่ยน และหลังตื่นจาก sleep |
| หน้าต่างบนสุดของ app อื่นมาบัง | ดันไอคอนกลับขึ้นบนสุดเมื่อหน้าต่างที่ active เปลี่ยน |
| Explorer รีสตาร์ท | เพิ่ม icon ที่ tray กลับ |
| ไฟดับตอนบันทึก | หัวข้อ 6 ความทนทาน |
| เปิด app ซ้ำ | ตัวที่รันอยู่เปิดหน้าตั้งค่าขึ้นมา ตัวใหม่ปิดตัวเอง |
| เป้าหมายของ link หายไป (ถอน app, ลบไฟล์) | ไอคอนยังอยู่ แสดงจาง 40% และมีเครื่องหมาย ! · กดแล้วแจ้งว่าไม่พบ พร้อมปุ่ม "แก้ไข" และ "ลบ" |
| ผู้ใช้ปิด "เปิดพร้อม Windows" ใน Task Manager | หัวข้อ 4.10 |
| ติดตั้งแล้วเจอ SmartScreen | คู่มือหัวข้อ "เริ่มต้นใช้งาน" |

---

## 8. บันทึกการตัดสินใจ

| วันที่ | เรื่อง | ตัดสินใจ | เหตุผล |
|---|---|---|---|
| 2026-09-14 | รูปแบบ | ไอคอนเดี่ยว ไม่มีแผงหลัก | กดได้ทันที วางกระจายได้ |
| 2026-09-14 | กลุ่ม | มีกลุ่ม ใช้ปนกับไอคอนเดี่ยว ไม่มีสวิตช์โหมด | สวิตช์ไม่เพิ่มความสามารถ แต่เพิ่มกรณีที่ตอบยาก |
| 2026-09-14 | link หลายที่ | แยก link กับของบนจอ | แก้ครั้งเดียวเปลี่ยนทุกที่ |
| 2026-09-14 | สไตล์ | Glass ค่าเริ่มต้น · HUD และ Dot Matrix เป็นตัวเลือก (เฟส 2) | ผู้ใช้เลือก |
| 2026-09-14 | ภาษา | ไทย + อังกฤษ | แจกคนอื่นด้วย |
| 2026-09-14 | การแจก | ฟรี open source MIT บน GitHub | ผู้ใช้เลือกฟรี/open source · MIT เข้ากับ library ที่ใช้ทั้งหมด |
| 2026-09-14 | เครื่องมือ | C# .NET 10 + WPF | app ค้างทั้งวัน ใช้ RAM น้อย ต้องคุยกับ Windows ลึก |
| 2026-09-14 | ตัวติดตั้ง | Velopack | อัปเดตตัวเองได้ ไม่ต้องใช้สิทธิ์ admin |
| 2026-09-14 | อัปเดต | มีตั้งแต่เวอร์ชันแรกที่แจก | ไม่อย่างนั้นคนที่ลงไปแล้วจะค้างที่เวอร์ชันแรก |
| 2026-09-14 | ถอนการติดตั้ง | ไม่ลบค่าตั้ง มีปุ่มลบในหน้าตั้งค่าแทน | Velopack แสดงหน้าถามระหว่างถอนไม่ได้ |
| 2026-09-14 | Windows 10 | รองรับแต่ไม่เบลอ | Acrylic ที่ใช้ได้เสถียรมีตั้งแต่ Windows 11 22H2 |
| 2026-09-14 | เซ็นโปรแกรม | ยังไม่เซ็น | เซ็นแล้วยังเตือนช่วงแรก · บริการราคาถูกของ Microsoft ไม่เปิดให้บุคคลในไทย |
| 2026-09-14 | ย้ายหลายตัว | ทำในเฟส 2 ไม่มีสวิตช์ | ใช้ได้เฉพาะโหมดแก้ไข ไม่รบกวนการใช้งานปกติ |
| 2026-09-15 | วิธีทำเบลอ (หัวข้อ 5.5) | เปลี่ยนจาก Acrylic ของระบบเป็น accent policy · หน้าตาที่ผู้ใช้เห็นเหมือนเดิม | ผล spike S4: Acrylic ของระบบเป็นพื้นทึบบนหน้าต่างที่ไม่รับ focus |
| 2026-09-15 | สไตล์ HUD และ Dot Matrix | ย้ายจากเฟส 2 มาเฟส 1 ทำครบทุกชิ้น (ไอคอน, กลุ่ม, เมนู, แถบเครื่องมือ, หน้าสแกน, หน้าตั้งค่า) ตามหัวข้อ 5.8, 5.9 | ผู้ใช้ขอให้เลือกได้ครบ 3 แบบตาม mockup |
| 2026-09-15 | หน้าตาหน้าตั้งค่า (หัวข้อ 4.8) | เปลี่ยนจากแบบ Windows 11 มาตรฐานเป็นแผ่นตามสไตล์ที่เลือก ใช้ตัวควบคุมแบบแผงปรับหน้าตาใน mockup (5.10) · ยังเป็นหน้าต่างปกติ | ผู้ใช้เลือก |
| 2026-09-15 | browser ของ link เว็บ (หัวข้อ 4.1) | เลือก browser ที่ติดตั้งในเครื่องได้ราย link ค่าเริ่มต้นคือ browser หลักของ Windows | ผู้ใช้ขอ |
| 2026-09-15 | ธีม (หัวข้อ 5.1) | เลือกได้ สว่าง/มืด/ตาม Windows · HUD ใช้สีมืดเสมอ | ตรงกับที่ตกลงไว้ใน DESIGN-DISCUSSION (หัวข้อ 13) · สีของ HUD ออกแบบมาสำหรับพื้นมืด |
| 2026-09-15 | เส้นแสงกวาดของ HUD (หัวข้อ 5.8) | เปลี่ยนจากล่างขึ้นบนเป็นบนลงล่าง | ผู้ใช้เลือกให้ตรงกับ mockup |
| 2026-09-16 | กระจกฝ้าเมื่อ Windows ไม่เบลอ (หัวข้อ 5.5) | ปิด Transparency effects ของ Windows หรือ Windows เก่า: แผ่น Glass วาดภาพพื้นหลัง desktop ที่เบลอไว้ใต้สีกระจกแทนพื้นทึบ 92% · พื้นทึบเหลือเฉพาะโหมดประหยัดแบตกับตอนปิดเบลอในหน้าตั้งค่า | เครื่องผู้ใช้ปิด Transparency effects อยู่ กระจกเลยเป็นแผ่นทึบ ผู้ใช้เลือกให้ยังดูเป็นกระจกโดยไม่ต้องเปิดค่าของ Windows · ยอมรับว่าเห็นแค่ภาพพื้นหลัง ไม่เห็นหน้าต่างอื่น |
| 2026-09-16 | แสงบนกระจก Glass (หัวข้อ 5.1–5.3, 5.5) | ขอบแสงไล่เฉียง สว่างที่มุมบนซ้ายและล่างขวา + เส้นนอกบาง + แสงเรืองที่มุม · แถวที่ชี้ ช่องที่เลือก ปุ่ม Accent และแผ่นโฟลเดอร์เป็นกระจกมันวาว · เส้นคั่นจางที่ปลาย · ไอคอนตัวอักษรใช้ขอบแสงแบบเดียวกันและแสงด้านบนไม่ตัดเป็นเส้น · ไม่มีเส้นขอบของระบบบนแผ่น Glass · ความทึบ `a` ของทุกชิ้นเท่าเดิม | ผู้ใช้ขอให้ Glass สวยขึ้นตามภาพอ้างอิง glassmorphism (magnific.com, แผ่นกระจกบนพื้นกรมท่ากับสี่เหลี่ยมสีส้ม) |
| 2026-09-16 | วิธีเพิ่มเว็บ (หัวข้อ 4.1, 4.7, 4.8) | ย้ายช่อง URL ออกจากหน้าสแกนไปเป็นหน้าต่าง "เพิ่มเว็บ" แยก: ที่อยู่ ชื่อ browser แล้วกดครั้งเดียวลงจอ · เปิดจากเมนู tray, ปุ่ม "เว็บ…" ในหน้าสแกน และหน้าตั้งค่า · พิมพ์ `youtube.com` ได้โดยไม่ต้องมี `https://` · ไม่อ่าน clipboard | ผู้ใช้วาง URL ในหน้าสแกนแล้วกด "เพิ่มลงจอ" โดยไม่กด Enter ก่อน URL หายเงียบ ๆ ได้แค่ app ที่ติ๊กไว้ แล้วบอกว่าวิธีเพิ่มเข้าใจยาก (ช่องอยู่ในหน้า "เพิ่มจากเครื่องนี้" ไม่มีชื่อกำกับ ต้องกด 2 ครั้ง) · ผู้ใช้เลือกหน้าต่างแยก (แทนแท็บหรือปรับหน้าเดิม) และเลือกไม่อ่าน clipboard · แทนการแก้ชั่วคราวที่ให้ "เพิ่มลงจอ" เพิ่ม URL ที่ค้างในช่อง ซึ่งยังไม่ได้ออกรุ่น |
| 2026-09-16 | icon ของ app (หัวข้อ 4.7) | เปลี่ยนจากลูกแก้วมันวาว 3 สีเป็นวงแหวนแสงรูปตัว Q ที่มีหางพุ่งเป็นดาวหาง (แบบ "วงโคจร") · ขนาด 16–24px วาดแยกให้หนาและเต็มกรอบ | ผู้ใช้อยากได้ icon ที่ล้ำขึ้น · เลือกจาก 5 แบบที่เทียบกันทั้งขนาดเต็มและขนาดจริงบน taskbar มืด/สว่าง (แสงสามสี, วงโคจร, เป้าล็อก, หลุดจากกลุ่ม, เม็ดจุด) · วงโคจรยังชัดที่ 16px เพราะเป็นรูปทรงเดียว และใช้สีเดิมของ Glass |
