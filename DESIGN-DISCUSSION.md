# MinkQuickLax — Design Discussion

> สถานะ: **ปิดการคุยออกแบบแล้ว (2026-09-14)** — ข้อสรุปทั้งหมดย้ายไปอยู่ที่ [docs/SPEC.md](docs/SPEC.md) และแผนลงมืออยู่ที่ [docs/PLAN.md](docs/PLAN.md) · **ถ้าขัดกัน ให้ยึด SPEC.md** · ไฟล์นี้เก็บไว้ดูเหตุผลย้อนหลัง
> อัปเดตล่าสุด: 2026-09-14

---

## 1. แนวคิดของ app

Quick launcher สำหรับ Windows ที่ลอยอยู่ **บนสุดของหน้าจอเสมอ (always on top)** เพื่อให้กดเปิด app หรือ link ได้เร็วที่สุด ไม่ว่าผู้ใช้กำลังทำอะไรอยู่

แนวเดียวกับ Dock ของ macOS, RocketDock หรือ ObjectDock แต่เน้นการเข้าถึงที่เร็วและไม่รบกวนงานที่ทำอยู่

---

## 2. ความต้องการตั้งต้น

- **Launcher ลอยบนสุด:** ตอนปกติเป็นปุ่มเล็ก ๆ เอาเมาส์ชี้แล้วกางออกมาแสดง link ทั้งหมดที่ลงทะเบียนไว้
  - → เปลี่ยนเป็นไอคอนเดี่ยว ไม่มีปุ่มให้กาง (ดูหัวข้อ 10)
- **โหมดใช้งาน:** ล็อกตำแหน่ง ใช้กดเปิด link
- **โหมดแก้ไข:** ลากไปวางตรงไหนของหน้าจอก็ได้ และจัดเรียง link ได้
- **ตั้งค่าซ่อน/แสดงได้:** *(ต้องยืนยัน ดูหัวข้อ 8)*
  - → ซ่อน/แสดงทั้งชุด เลือกวิธีได้ในหน้าตั้งค่า (หัวข้อ 13)
- **Mode สำหรับหลาย link:** *(ต้องยืนยันความหมาย ดูหัวข้อ 8)*
  - → ไอคอนเดี่ยวได้หลายตัว และกลุ่มได้หลายกลุ่ม ใช้ปนกันได้ (หัวข้อ 12)
- **สแกนหา app ในเครื่อง:** เลือก app มาลงทะเบียน แก้ชื่อได้ ใช้ icon เดิมของ app หรือตั้ง icon เองได้
- **เป็น Windows app** (native)

### ความต้องการรอบ 2 (2026-09-14)

| ความต้องการ | ผลตรวจ | ดูหัวข้อ |
|---|---|---|
| มีตัวติดตั้ง .exe | ทำได้ ปรับเป็นติดตั้งแบบรายผู้ใช้ ไม่ต้องใช้สิทธิ์ admin | 11 |
| เปิดอัตโนมัติตอนเปิดเครื่อง | ปรับเป็น "ตอนเข้าสู่ระบบ" และตัว app ต้องไม่รันเป็น admin | 11 |
| หน้าตั้งค่าที่ครบถ้วน | ทำได้ ปรับเป็นแบ่งหมวด มีช่องค้นหา และซ่อนค่าขั้นสูงไว้ | 13 |
| คู่มือเป็น HTML สวย ค้นหาได้ | ทำได้ ต้องใช้ได้แบบไม่มีเน็ต และค้นหาภาษาไทยต้องทำเอง | 14 |
| เลือกใช้แบบเดี่ยว แบบกลุ่ม หรือทั้งคู่ | ปรับเป็น ไม่ต้องมีสวิตช์เลือกโหมด ใช้ปนกันได้เสมอ | 12 |
| มีปุ่มเดี่ยวได้มากกว่าหนึ่ง | แบบไอคอนเดี่ยวรองรับอยู่แล้ว | 10, 12 |
| ลากไปวางตรงไหนของจอก็ได้ | ทำได้ ปรับค่าเริ่มต้นเป็นไม่วางทับ taskbar | 12 |

---

## 3. ปัญหาที่ app แบบนี้มักเจอ

ควรออกแบบกันไว้ตั้งแต่ต้น

| ปัญหา | ทางแก้ |
|---|---|
| บังจอตอนเล่นเกม ดูหนังเต็มจอ หรือ present งาน | ตรวจว่ามี app เต็มจออยู่ แล้วซ่อนตัวเองอัตโนมัติ |
| เมาส์แค่ผ่านแต่ launcher กางเอง | รอประมาณ 200ms ก่อนกาง และหุบทันทีเมื่อเมาส์ออก |
| กดแล้วแย่ง focus (เช่นกำลังพิมพ์งานอยู่) | ตั้ง window แบบ no-activate (`WS_EX_NOACTIVATE`) กดได้โดยไม่ดึง focus ไปจาก app ที่ใช้อยู่ |
| ใช้หลายจอ, scaling ไม่เท่ากัน, เปลี่ยนความละเอียดจอ | จำตำแหน่งแยกตามจอ และดึงกลับมาถ้าหลุดขอบจอ |
| โดน window อื่นที่ตั้งบนสุดไว้เหมือนกันบัง (เช่น Task Manager) | สั่งให้ launcher กลับขึ้นบนสุดเป็นระยะ |
| launcher โผล่ใน taskbar หรือ Alt+Tab | ใช้ window แบบ tool window (`WS_EX_TOOLWINDOW`) |
| ถอดจอที่มีไอคอนวางอยู่ | ย้ายไอคอนมาจอหลักชั่วคราวแต่จำตำแหน่งเดิมไว้ เสียบจอกลับแล้วคืนที่เดิม |
| ตอนเข้า Windows หรือตื่นจาก sleep จอยังไม่พร้อม ไอคอนเลยไปผิดที่ | จัดตำแหน่งใหม่ทุกครั้งที่จอหรือ DPI เปลี่ยน (`WM_DISPLAYCHANGE`, `WM_DPICHANGED`) |
| Explorer รีสตาร์ทแล้ว icon ที่ tray หาย | ดักข้อความ `TaskbarCreated` แล้วเพิ่ม icon กลับเข้าไป |
| ไฟดับตอนกำลังบันทึก ไฟล์ค่าตั้งเสีย | เขียนลงไฟล์ชั่วคราวก่อนแล้วค่อยแทนที่ และเก็บสำรองย้อนหลัง 10 ชุด |
| เปิด app ซ้ำ ได้ไอคอนซ้อนกันสองชุด | เปิดได้ตัวเดียว ถ้าเปิดซ้ำให้ตัวที่รันอยู่เปิดหน้าตั้งค่าแทน |

---

## 4. ข้อเสนอเพิ่มเติม

### 4.1 คีย์ลัด (Global Hotkey) คู่กับ hover — แนะนำมาก
- การ hover ต้องเลื่อนเมาส์ไปหาจุดนั้นก่อนทุกครั้ง
- กดคีย์ลัด (ค่าเริ่มต้น `Win+Alt+Q` ผู้ใช้ตั้งเองได้) แล้ว launcher เด้งขึ้น **ตรงตำแหน่งเมาส์**
- กดเลข 1–9 เพื่อเปิด หรือพิมพ์ค้นหาได้ทันที
- เร็วกว่า hover และใช้ได้แม้ตอนเปิด app เต็มจออยู่

**ค่าเริ่มต้น `Win+Alt+Q`**
- **Q** มาจาก Quick กดได้ด้วยมือซ้ายมือเดียว
- Windows 11 ไม่ได้ใช้คีย์นี้ ([รายการคีย์ลัดของ Microsoft](https://support.microsoft.com/en-us/windows/keyboard-shortcuts-in-windows-dcc61a57-8ff0-cffe-9796-cb9706c75eec)) และค่าตั้ง PowerToys ในเครื่องก็ไม่ได้ใช้
- ลองลงทะเบียนบนเครื่องจริงแล้ว ว่าง (2026-09-14)
- **ข้อเสีย:** คีย์บอร์ดเกมมิ่งบางรุ่นและบางเกมปิดปุ่ม Win ตอนเล่น ถ้าต้องใช้ในเกมให้เปลี่ยนเป็นคีย์อื่น

**คีย์ที่ไม่เลือก เพราะชนกับของที่มีอยู่**

| คีย์ | ชนกับ |
|---|---|
| `Ctrl+Alt+Space` | Claude Desktop |
| `Alt+Space` | PowerToys Run, Copilot และเมนูหน้าต่างของ Windows |
| `Win+Alt+Space` | PowerToys Command Palette |
| `Ctrl+Space` | PowerToys Peek, สลับ IME, autocomplete ใน editor |
| `Ctrl+Shift+…` | CapsLock (AutoHotkey แมปเป็น Ctrl+Shift) และคีย์ลัดใน app ทั่วไป เช่น VS Code |
| `Win+Space`, `Alt+Shift+…`, ปุ่ม `` ` `` | สลับภาษาไทย/อังกฤษ |
| `Ctrl+Alt+ตัวอักษร` | คีย์บอร์ดยุโรปใช้แทน AltGr ไว้พิมพ์ตัวอักษร เช่น `@` ในภาษาเยอรมัน |
| `Win+Alt+` B, D, G, H, K, M, R, T, U, P | Windows (HDR, วันเวลา, Game Bar, ไมค์) และ PowerToys (Cursor Wrap, Crosshairs) |

**ถ้ายังชนอยู่ดี**
- **ตรวจเจอไม่ได้ทุกกรณี:** `RegisterHotKey` แจ้ง error เฉพาะตอนที่ app อื่นจองคีย์ด้วยวิธีเดียวกัน ส่วน app ที่ดักคีย์ผ่าน keyboard hook (เช่น Claude Desktop, AutoHotkey, PowerToys บางตัว) ตรวจไม่เจอ
  - ลองแล้ว: `Ctrl+Alt+Space` ลงทะเบียนผ่านทั้งที่ Claude Desktop เปิดอยู่
- **ถ้าลงทะเบียนไม่ผ่าน:** แจ้งเตือนที่ tray แล้วเปิดหน้าตั้งคีย์ลัดให้ทันที
- **หน้าตั้งคีย์ลัด:** กดคีย์ที่อยากใช้เพื่อบันทึก ถ้าตรงกับตารางข้างบนให้ขึ้นเตือน
- **ปิดคีย์ลัดได้:** สำหรับคนที่ไม่ใช้

### 4.2 ติดขอบจอ (Edge dock) เป็นทางเลือก
- ขอบจอเป็นจุดที่เมาส์ไปถึงเร็วที่สุด เพราะเลื่อนเกินไปก็ไม่หลุด
- ตอนหุบเหลือแค่แถบบาง ๆ ไม่บังงาน
- ให้เลือกได้ระหว่างแบบลอยอิสระกับแบบติดขอบจอ

### 4.3 ลากไฟล์มาวางเพื่อลงทะเบียน (Drag & Drop)
- ลากไฟล์ shortcut หรือ URL จาก browser มาวางบน launcher แล้วลงทะเบียนเลย
- ไม่ต้องเปิดหน้าตั้งค่า

### 4.4 รองรับมากกว่า app
- ประเภท link: app, ไฟล์, โฟลเดอร์, URL, คำสั่ง/script, หน้าตั้งค่า Windows (`ms-settings:`)
- ตัวเลือกเพิ่มเติม: Run as admin, arguments, working directory

### 4.5 ไอเดียสำหรับทีหลัง
- **เปิดหลาย app ทีเดียว (Launch set):** เช่นชุดเริ่มงาน VS Code + Chrome + Slack
- **จุดบอกว่า app ไหนเปิดอยู่:** กดแล้วสลับไปหน้าต่างเดิมแทนการเปิดซ้ำ (เลือกเปิด/ปิดได้)
- **นำเข้า/ส่งออกค่าตั้ง:** ใช้สำรองข้อมูลหรือย้ายไปเครื่องใหม่

---

## 5. ขั้นตอนสแกนหา app ในเครื่อง

### แหล่งที่ดึงรายชื่อ
- **`shell:AppsFolder`:** รายการเดียวกับ "All apps" ใน Start Menu ได้ทั้งโปรแกรมทั่วไป (Win32) และ app จาก Microsoft Store
- **Shortcut บน Desktop:** ใช้เสริม

### หน้าจอเลือก app
- แสดงรายการพร้อม icon
- มีช่องค้นหา
- ติ๊กเลือกหลายตัวแล้วเพิ่มทีเดียวได้
- แก้ชื่อได้ก่อนหรือหลังเพิ่ม

### ที่มาของ icon
1. ดึงจาก app แบบความละเอียดสูง (256px)
2. อัปโหลดไฟล์ PNG, ICO หรือ SVG เอง
3. ใช้ favicon ของเว็บ สำหรับ link ที่เป็น URL (ดึงจากเว็บนั้นโดยตรง ไม่ผ่านบริการภายนอก)
4. ใช้ตัวอักษรบนพื้นสี กรณีที่ไม่มี icon

---

## 6. เครื่องมือที่ใช้สร้าง

| ตัวเลือก | ข้อดี | ข้อเสีย |
|---|---|---|
| **C# .NET 10 + WPF** ⭐ แนะนำ | เป็น app Windows แท้ ใช้ RAM น้อย ทำ window โปร่งใส ลอยบนสุด คีย์ลัด และดึง icon ได้ง่าย | ต้องแต่ง UI เพิ่มให้ดูทันสมัย (มี library WPF-UI ช่วย) |
| WinUI 3 | หน้าตาเหมือน Windows 11 (Mica/Acrylic) | ทำ window โปร่งใสแบบรูปทรงอิสระได้ยากกว่า |
| Tauri (Rust + HTML/CSS) | เขียน UI ด้วย HTML/CSS ได้ ตัวโปรแกรมเล็ก | งานที่ต้องคุยกับ Windows ลึก ๆ ต้องเขียนด้วย Rust |
| Electron | ง่ายถ้าถนัดเขียนเว็บ | ใช้ RAM 150MB ขึ้นไป ไม่เหมาะกับ app ที่เปิดค้างไว้ตลอด |

**เหตุผลที่แนะนำ C# + WPF:** เหมาะกับ app ที่ต้องเปิดค้างไว้ตลอดและต้องใช้ความสามารถของ Windows เยอะ

### ส่วนประกอบหลักของโปรแกรม (ร่าง)
- **Icon window:** 1 บานต่อ 1 ไอคอน ลอยบนสุด โปร่งใส ไม่แย่ง focus ไม่โผล่ใน taskbar
- **Group window:** 1 บานต่อ 1 กลุ่ม ส่วนแผงที่กางออกมาสร้างตอนกางเท่านั้น (หัวข้อ 12)
- **Settings window:** จัดการ link และค่าตั้งต่าง ๆ (หัวข้อ 13)
- **Manual window:** แสดงคู่มือ HTML ผ่าน WebView2 (หัวข้อ 14)
- **Startup manager:** จัดการการเปิดพร้อม Windows ให้ตรงกับ Task Manager (หัวข้อ 11)
- **Single instance:** กันเปิดซ้ำด้วย Mutex และส่งคำสั่งให้ตัวที่รันอยู่ผ่าน named pipe
- **Installer / Updater:** Velopack (หัวข้อ 11)
- **Style:** 1 สไตล์ = ResourceDictionary 1 ชุด สลับได้ทันทีโดยไม่ต้องเปิด app ใหม่ (หัวข้อ 9)
- **Localization:** ข้อความทั้งหมดอยู่ในไฟล์ `.resx` ภาษาไทยและอังกฤษ (หัวข้อ 15)
- **App scanner:** สแกนหา app ในเครื่อง
- **Icon cache:** เก็บ icon ที่ดึงมาแล้ว ไม่ต้องดึงซ้ำ
- **Tray icon:** คลิกเพื่อซ่อน/แสดง คลิกขวาเปิดเมนู: เพิ่มจากเครื่องนี้ / โหมดแก้ไข / จัดเรียงใหม่ / หน้าตั้งค่า / ออกจากโปรแกรม
- **Hotkey service:** ลงทะเบียนคีย์ลัด
- **ที่เก็บค่าตั้ง:** ไฟล์ JSON ใน `%AppData%\MinkQuickLax\`

---

## 7. แผนแบ่งเฟส

### เฟส 1 — MVP
- [ ] ไอคอนเดี่ยวลอยบนสุด 1 หน้าต่างต่อ 1 ไอคอน
- [ ] แยก link กับของที่วางบนจอ link เดียววางได้หลายที่ (หัวข้อ 12)
- [ ] กลุ่มแบบโฟลเดอร์ ลาก link เข้า/ออกกลุ่มได้ (หัวข้อ 12)
- [ ] โหมดแก้ไข ลากไอคอนและกลุ่มไปวางได้อิสระ ชิดกริดและชิดแนวไอคอนอื่น
- [ ] Ctrl+ลาก ย้ายทีละตัวได้โดยไม่ต้องเข้าโหมดแก้ไข
- [ ] เพิ่ม link ประเภท app, ไฟล์, โฟลเดอร์, URL
- [ ] สแกนหา app ในเครื่อง
- [ ] แก้ชื่อและ icon ได้
- [ ] เมนูคลิกขวาที่ไอคอน
- [ ] icon ที่ tray พร้อมเมนูคลิกขวา · คลิกเพื่อซ่อน/แสดงทั้งชุด
- [ ] ระบบสลับสไตล์ และทำสไตล์แรกให้ครบ (หัวข้อ 8 ข้อ 10)
- [ ] ข้อความใน app ภาษาไทยและอังกฤษ (หัวข้อ 15)
- [ ] หน้าตั้งค่า หมวด Link, กลุ่ม, หน้าตา, ซ่อน/แสดง, การใช้งาน, ตำแหน่ง, ทั่วไป, เกี่ยวกับ (หัวข้อ 13)
- [ ] เปิดพร้อม Windows มีสวิตช์ในหน้าตั้งค่า และตรงกับ Task Manager (หัวข้อ 11)
- [ ] เก็บค่าตั้งเป็นไฟล์ JSON พร้อมสำรองอัตโนมัติ
- [ ] ตัวติดตั้ง .exe และตัวถอนการติดตั้ง (หัวข้อ 11)
- [ ] อัปเดตอัตโนมัติ แยกช่องทาง beta/stable (หัวข้อ 11)
- [ ] คู่มือ HTML: โครงหน้า ระบบค้นหา สลับภาษา และเนื้อหาภาษาไทยของฟีเจอร์เฟส 1 (หัวข้อ 14)

### เฟส 2
- [ ] คีย์ลัด (Global Hotkey) ทั้งคีย์เรียก launcher และคีย์ซ่อน/แสดงทั้งชุด
- [ ] สไตล์ที่สอง
- [ ] เนื้อหาคู่มือภาษาอังกฤษ
- [ ] ~~แบ่งกลุ่ม / แท็บ~~ → ย้ายไปเฟส 1 เป็นกลุ่มแบบโฟลเดอร์
- [ ] กลุ่มแบบแถบ (หัวข้อ 12)
- [ ] ซ่อนอัตโนมัติตอนเปิด app เต็มจอ
- [ ] ลากไฟล์มาวางเพื่อลงทะเบียน
- [ ] ติดขอบจอ (Edge dock)
- [ ] หน้าต้อนรับครั้งแรก (หัวข้อ 15)
- [ ] เลือกหลายไอคอนแล้วลากพร้อมกัน
- [ ] ปุ่ม ⓘ ในหน้าตั้งค่าเปิดคู่มือตรงหัวข้อนั้น
- [ ] ~~อัปเดตอัตโนมัติ~~ → ย้ายไปเฟส 1 เพราะต้องมีตั้งแต่เวอร์ชันแรกที่แจก

### เฟส 3
- [ ] เปิดหลาย app ทีเดียว (Launch set)
- [ ] จุดบอกว่า app เปิดอยู่
- [ ] รองรับหลายจอเต็มรูปแบบ
- [ ] นำเข้า/ส่งออกค่าตั้ง
- [ ] ธีม
- [ ] ~~ภาษาอังกฤษ~~ → ข้อความใน app อยู่เฟส 1 ส่วนเนื้อหาคู่มืออยู่เฟส 2

---

## 8. คำถามที่ต้องตอบก่อนเริ่ม

1. **ซ่อน/แสดง**
   - "ตั้งค่าซ้อนแสดงได้" หมายถึง "ซ่อน/แสดง" ใช่ไหม?
   - อยากสั่งผ่านอะไร: คีย์ลัด, icon ที่ tray หรือให้ซ่อนเองอัตโนมัติ?
   - คำตอบ: **ทำเป็นตัวเลือกในหน้าตั้งค่า** มีครบทุกวิธี เปิด/ปิดแยกกันได้ (หัวข้อ 13)

2. **Mode หลาย link หมายถึงแบบไหน**
   - (ก) แบ่ง link เป็นกลุ่มหรือแท็บเมื่อมีเยอะ
   - (ข) มี launcher หลายตัว วางคนละจุดบนจอ
   - หมายเหตุ: แบบไอคอนเดี่ยวได้ (ข) มาในตัวแล้ว เหลือแค่ว่ายังต้องมีกลุ่มหรือแท็บ (ก) ด้วยไหม
   - คำตอบ: **ทั้งคู่** ไอคอนเดี่ยวได้หลายตัว กลุ่มได้หลายกลุ่ม และใช้ปนกันได้ (หัวข้อ 12)

3. **หน้าตาตอนหุบ**
   - ปุ่มกลมลอย
   - แถบติดขอบจอ
   - dock เล็กที่เห็น icon ย่อ
   - ไอคอนเดี่ยว
   - คำตอบ: **ไอคอนเดี่ยว** link แต่ละตัวเป็นไอคอนลอยแยกกัน ไม่มีแผง (ดูหัวข้อ 10)

4. **ผู้ใช้**
   - ใช้เองคนเดียว หรือจะแจกคนอื่น?
   - ถ้าแจก ต้องคิดเรื่องตัวติดตั้งและการเซ็นโปรแกรม (code signing) ไม่อย่างนั้น Windows SmartScreen จะขึ้นเตือน
   - ข้อนี้สำคัญขึ้นเพราะตอนนี้ต้องมีตัวติดตั้งแล้ว คำตอบใช้ตัดสินใจเรื่องการเซ็นโปรแกรมและระบบอัปเดตอัตโนมัติ (หัวข้อ 11)
   - คำตอบ: **ใช้เองและแจกคนอื่นด้วย** สิ่งที่ต้องทำเพิ่มดูหัวข้อ 11 "แจกให้คนอื่น"

5. **การเขียนโค้ด**
   - เขียนโปรแกรมภาษาไหนเป็นบ้าง?
   - ให้ Claude เขียนทั้งหมด หรืออยากแก้ต่อเองด้วย? (มีผลกับการเลือกเครื่องมือ)
   - คำตอบ: **ให้ Claude ดำเนินการเองทั้งหมด และเลือกเครื่องมือที่เหมาะกับงานที่สุด** → C# .NET 10 + WPF (รายละเอียดใน docs/PLAN.md หัวข้อ 1)

6. **ตัดสวิตช์โหมด "เดี่ยว / กลุ่ม / ทั้งคู่" ออก**
   - ให้ใช้ไอคอนเดี่ยวกับกลุ่มปนกันได้เสมอแทน เหตุผลอยู่ในหัวข้อ 12
   - ตกลงไหม?
   - คำตอบ: **ตกลง** ตัดสวิตช์ออก

7. **link เดียววางได้หลายที่**
   - เช่น Chrome เป็นไอคอนเดี่ยว และอยู่ในกลุ่ม "งาน" ด้วย (หัวข้อ 12)
   - ต้องการไหม?
   - คำตอบ: **ต้องการ**

8. **ภาษา**
   - app และคู่มือ ใช้ภาษาไทยอย่างเดียว หรือต้องมีภาษาอังกฤษด้วย?
   - ถ้าจะมีภาษาอังกฤษทีหลัง ก็ควรเก็บข้อความเป็นไฟล์แปลตั้งแต่เฟส 1 (หัวข้อ 15)
   - คำตอบ: **ไทยและอังกฤษ** (หัวข้อ 14, 15)

9. **แจกแบบไหน และวางไฟล์ให้ดาวน์โหลดที่ไหน**
   - แจกฟรีหรือขาย? เปิดโค้ดให้คนอื่นดูด้วยไหม?
   - มีผลกับที่เก็บไฟล์ติดตั้งและไฟล์อัปเดต (หัวข้อ 11)
     - GitHub Releases ต้องเป็น repo สาธารณะ คนอื่นถึงจะโหลดได้
     - ถ้าอยากเก็บโค้ดไว้เป็นส่วนตัว ให้แยก repo สาธารณะอีกอันไว้วางแค่ไฟล์ติดตั้ง
   - คำตอบ: **ฟรีและ open source** → สัญญาอนุญาต MIT · โค้ดและไฟล์ติดตั้งอยู่ใน repo สาธารณะเดียวกันบน GitHub

10. **สไตล์ไหนทำก่อน และใช้เป็นค่าเริ่มต้น**
    - มีทั้ง HUD และ Dot Matrix ให้เลือกในหน้าตั้งค่า แต่ UI ทุกชิ้นต้องทำสองแบบ: ไอคอน, ชื่อ, เมนู, แผงกลุ่ม, หน้าคู่มือ
    - ข้อเสนอ: เฟส 1 ทำระบบสลับสไตล์กับสไตล์แรกให้ครบ แล้วทำสไตล์ที่สองในเฟส 2 (หัวข้อ 9)
    - คำตอบ: **Glass** → Glass เป็นค่าเริ่มต้นและทำในเฟส 1 · HUD และ Dot Matrix เป็นตัวเลือกในเฟส 2 (ค่าของ Glass อยู่ใน docs/SPEC.md หัวข้อ 5)

---

## 9. สไตล์หน้าตา

- **ต้องการ:** ดูเป็น technology และล้ำกว่า Glassmorphism
- **สถานะ:** **Glass** เป็นค่าเริ่มต้นและทำในเฟส 1 · HUD และ Dot Matrix เป็นตัวเลือกในหน้าตั้งค่า ทำในเฟส 2 (หัวข้อ 8 ข้อ 10)
- **การสลับสไตล์**
  - สลับแล้วเห็นผลทันที ไม่ต้องเปิด app ใหม่
  - เปลี่ยนทั้งไอคอน ชื่อ เมนู แผงกลุ่ม สีเน้นของหน้าตั้งค่า และหน้าคู่มือ
  - ใช้ลูกเล่น "สแกนไล่ทีละไอคอน" ตอนสลับ (ดูลูกเล่นด้านล่าง)
  - ฟอนต์ทั้ง 4 ตัวใช้สัญญาอนุญาต OFL ฝังไปกับ app และแจกต่อได้
- **Mockup (ลองกดได้):** https://claude.ai/code/artifact/0a7e4095-33f8-4eb2-8301-298bd6f00bd2
  - v0.3: เพิ่มสไตล์ HUD และ Dot Matrix สลับได้ที่แผงด้านขวา
  - v0.2: แบบไอคอนเดี่ยว (หัวข้อ 10)
  - v0.1: Glass และหน้าตาแบบแผง 3 แบบ (ยังสลับดูได้)

### ตัวเลือก A: HUD
- **แนวคิด:** หน้าจอแสดงข้อมูลแบบในยานอวกาศหรือหมวกนักบิน
- **ไอคอน:** วางบนแผ่นตัดมุม พื้นดำอมน้ำเงิน เส้นขอบสีฟ้า cyan `#6FF3FF`
- **ตอนพัก:** ไอคอนเป็นโฮโลแกรมสีฟ้าพร้อมเส้นสแกน จอเลยไม่รก
- **เมาส์เข้าใกล้:** สีจริงค่อย ๆ กลับมา และแผ่นเรืองแสงขึ้น
- **ชี้ที่ไอคอน:** วงเล็บล็อกเป้า 4 มุม และข้อมูลขึ้นทีละตัวอักษร (ลำดับ, ประเภท, ชื่อไฟล์, สถานะ)
- **โหมดแก้ไข:** วงเล็บเปลี่ยนเป็นสีส้มกะพริบแทนการสั่น
- **ตัวอักษร:** Chakra Petch (มีภาษาไทย) และ JetBrains Mono

### ตัวเลือก B: Dot Matrix
- **แนวคิด:** อุปกรณ์อิเล็กทรอนิกส์แบบมินิมอล คล้ายจอ LED เม็ดจุด
- **ไอคอน:** วงกลม ตัดเป็นเม็ดจุดบนตาราง 4px
- **ตอนพัก:** ขาวดำ เม็ดจุดเล็ก
- **เมาส์เข้าใกล้:** จุดขยายจนติดกันเต็ม และสีจริงกลับมา
- **สถานะ:** จุดแดง = app ที่เปิดอยู่ · ชี้แล้วมีวงจุดหมุนรอบไอคอน
- **ชื่อไอคอนและเมนู:** แผ่นทึบสีขาว (หรือดำตามธีม Windows) ตัวเลขลำดับแบบเม็ดจุด
- **ตัวอักษร:** Doto (ตัวเลข) และ IBM Plex Sans Thai

### ตัวเลือก C: Glass (เดิม)
- blur 28px, saturate 170% · สีกระจกทึบ 18% · ขอบขาว 28% · มุมโค้ง 22px

### ลูกเล่นที่ใช้ได้ทุกสไตล์
- ไอคอนจางลงตอนไม่ได้ใช้ และชัดขึ้นเมื่อเมาส์เข้าใกล้
- ไอคอนขยายตามเมาส์
- เส้นช่วยจัดแนวและพิกัดตอนลากไอคอน
- ไอคอนเด้งตอนเปิด app
- เปลี่ยนสไตล์แล้วมีการสแกนไล่ทีละไอคอน

### ข้อจำกัดของ Windows ที่มีผลกับดีไซน์ (ต้องทดสอบบนเครื่องจริง)
- **การเบลอสิ่งที่อยู่ข้างหลังหน้าต่างต้องให้ DWM ทำ**
  - Win11: `DWMWA_SYSTEMBACKDROP_TYPE` (Acrylic)
  - Win10: `SetWindowCompositionAttribute` + `ACCENT_ENABLE_ACRYLICBLURBEHIND`
  - `BlurEffect` ของ WPF เบลอได้แค่ของที่อยู่ในหน้าต่างตัวเอง
- **Acrylic ของระบบได้แค่หน้าต่างสี่เหลี่ยมมุมโค้ง 8px**
  - ปุ่มกลมที่เบลอด้วยต้องตัดขอบด้วย region ซึ่งทำให้ขอบหยัก
- **โฮโลแกรม (HUD) และเม็ดจุด (Dot Matrix):** เตรียมภาพไอคอนไว้ 2 ชุดใน icon cache แล้วค่อย ๆ เปลี่ยนความทึบ หรือเขียน `ShaderEffect` (HLSL) ของ WPF
- **แสงเรือง วงเล็บล็อกเป้า และวงจุด:** อยู่นอกตัวไอคอน ต้องเผื่อขอบหน้าต่างรอบละ 12px
- **ทางที่เสนอ**
  - แบบไอคอนเดี่ยว: ตัวไอคอนไม่ต้องเบลอเลย ใช้แผ่นพื้นแค่กับชื่อไอคอน เมนูคลิกขวา คีย์ลัด และหน้าสแกน
  - แบบแผง ตอนกาง: ใช้ Acrylic จริง แล้ววางสีกับขอบของเราทับ
  - แบบแผง ตอนหุบ: วาดกระจกเองแบบไม่เบลอ
- **ถ้าผู้ใช้ปิด Transparency effects หรือเปิดโหมดประหยัดแบต:** เปลี่ยนเป็นพื้นทึบ

---

## 10. รูปแบบที่เลือก: ไอคอนเดี่ยว

- **แนวคิด:** link แต่ละตัวเป็นไอคอนลอยแยกกัน ไม่มีปุ่มหรือแผงให้กาง วางไว้ตรงไหนของจอก็ได้ กดเปิดได้ทันที
- **ลองกดได้ใน mockup v0.2** (ลิงก์ในหัวข้อ 9)

### การใช้งาน
- **โหมดใช้งาน**
  - ตำแหน่งล็อกไว้ กดไอคอนเพื่อเปิด ถ้า app เปิดอยู่แล้วจะสลับไปหน้าต่างเดิม
  - ชี้ค้าง 350ms แล้วชื่อขึ้น หรือตั้งให้แสดงชื่อใต้ไอคอนตลอด
  - คลิกขวาที่ไอคอน: เปิด, เปิดในฐานะผู้ดูแลระบบ, เปิดตำแหน่งที่เก็บ (ไฟล์/โฟลเดอร์), ย้ายตำแหน่ง, นำออก
- **โหมดแก้ไข**
  - ไอคอนทุกตัวสั่น ลากไปวางตรงไหนก็ได้
  - วางแล้วชิดกริด 24px หรือชิดแนวไอคอนอื่นเมื่อห่างไม่เกิน 8px (มีเส้นช่วยจัดแนวให้เห็น)
  - วางทับกันไม่ได้ ถ้าวางทับ ไอคอนจะขยับไปช่องว่างที่ใกล้ที่สุด
    - ยกเว้นลากค้างไว้บนไอคอนอื่น 0.6 วินาที จะสร้างเป็นกลุ่ม (หัวข้อ 12)
  - กดลูกศรเพื่อขยับทีละ 24px และกด Delete เพื่อนำออก
  - กด Ctrl+Z เพื่อย้อนกลับ
  - มีแถบเครื่องมือด้านบนจอ: เพิ่ม, จัดเรียงใหม่, ยกเลิก (คืนตำแหน่งทั้งหมดก่อนเข้าโหมด), เสร็จ
- **ทางเข้าเมนูเพิ่ม link และโหมดแก้ไข** (เพราะไม่มีแผงให้กดแล้ว)
  - คลิกขวาที่ icon ใน tray
  - คลิกขวาที่ไอคอน
  - ลากไฟล์มาวาง (เฟส 2)
- **ค่าตั้ง:** ขนาดไอคอน 32–64px, ความทึบตอนไม่ได้ใช้, แสดงชื่อใต้ไอคอน

### ข้อเสียและทางแก้

| ข้อเสีย | ทางแก้ |
|---|---|
| บังจอหลายจุด | จางลงตอนไม่ได้ใช้ (ค่าเริ่มต้นทึบ 70%) และชัดขึ้นเมื่อเมาส์เข้าใกล้ 130px · ซ่อนทั้งชุดได้จาก tray |
| link เยอะแล้วจอรก | รวมเป็นกลุ่ม (หัวข้อ 12) · ค้นหา link ทั้งหมดผ่านคีย์ลัด `Win+Alt+Q` (หัวข้อ 4.1) |
| ไม่มีแผงให้กดเพิ่มหรือแก้ไข | ใช้เมนูคลิกขวาที่ไอคอนและที่ tray |
| ต้องจำตำแหน่งทุกตัว | เก็บตำแหน่งเป็นสัดส่วนของพื้นที่จอ แยกตามจอ เปลี่ยนความละเอียดแล้วไม่หลุดขอบ · มีปุ่มจัดเรียงใหม่ |
| ตอนเปิด app เต็มจอต้องซ่อนทุกตัว | ซ่อนและแสดงพร้อมกันทั้งชุด |

### วิธีทำบน Windows (ร่าง)
- **ไอคอน 1 ตัว = หน้าต่าง WPF 1 บาน** ขนาดเท่าไอคอนบวกเงารอบละ 12px (ไอคอน 48px → หน้าต่าง 72×72)
- **หน้าต่างโปร่งใส** (`AllowsTransparency="True"` + `WindowStyle="None"`)
  - ไม่ต้องใช้ Acrylic ขอบไอคอนเนียน ไม่เจอปัญหาขอบหยักในหัวข้อ 9
  - ส่วนที่โปร่งใสคลิกทะลุไปถึงหน้าต่างข้างหลังได้เอง
  - ถ้าอยากให้รับเมาส์รอบไอคอนด้วย ให้ใส่พื้นหลัง `#01000000`
- **ทุกบานต้องตั้ง**
  - `Topmost` ลอยบนสุด
  - `WS_EX_TOOLWINDOW` ไม่โผล่ใน taskbar และ Alt+Tab
  - `WS_EX_NOACTIVATE` ไม่แย่ง focus
- **ชื่อไอคอนและเมนูคลิกขวา** เป็นหน้าต่างลอยอีกบาน ถ้าอยากให้เบลอต้องใช้ Acrylic ของระบบ (มุมโค้งแค่ 8px)
- **ซ่อน/แสดง ดึงกลับขึ้นบนสุด และจำตำแหน่ง** ต้องทำกับทุกบานเป็นชุด
- **ต้องทดสอบบนเครื่องจริง:** เปิดไอคอน 20–30 ตัวแล้วดูว่าใช้ RAM และ CPU เท่าไร

### คำถามที่ยังค้าง
1. **ยังต้องมีแผงด้วยไหม**
   - เช่นให้ตัวที่ใช้บ่อยเป็นไอคอนเดี่ยว ส่วนที่เหลือเก็บไว้ในแผง และลากเข้า/ออกได้
   - คำตอบ: **ต้องมี** ในรูปแบบกลุ่ม (หัวข้อ 12)
2. **ย้ายทีละหลายตัว**
   - ต้องเลือกหลายไอคอนแล้วลากพร้อมกันได้ไหม
   - ข้อเสนอ: ในโหมดแก้ไขลากเป็นกรอบเพื่อเลือกหลายตัว หรือกด Ctrl+คลิกทีละตัว (เฟส 2) · ถ้าย้ายด้วยกันบ่อย ๆ ให้รวมเป็นกลุ่มแทน
   - คำตอบ: **ทำ** (เฟส 2)
     - ไม่ใส่สวิตช์ในหน้าตั้งค่า เพราะเลือกหลายตัวได้เฉพาะในโหมดแก้ไข ไม่ไปรบกวนการใช้งานปกติอยู่แล้ว
     - ถ้าต้องการสวิตช์จริง ๆ เพิ่มในหมวดตำแหน่งได้

---

## 11. ตัวติดตั้งและการเปิดพร้อม Windows

### ตัวติดตั้ง (.exe)

| ตัวเลือก | ข้อดี | ข้อเสีย |
|---|---|---|
| **Velopack** ⭐ แนะนำ | ได้ `Setup.exe` ติดตั้งเสร็จในคลิกเดียว · มีระบบอัปเดตอัตโนมัติ โหลดเฉพาะส่วนที่เปลี่ยน · ดึงไฟล์อัปเดตจาก GitHub Releases ได้ | ไม่มีหน้าวิซาร์ด ตัวเลือกต่าง ๆ ต้องย้ายไปไว้ในหน้าต้อนรับของ app (หัวข้อ 15) |
| Inno Setup | ได้ `Setup.exe` แบบมีหน้าวิซาร์ดคุ้นตา ติ๊ก "เปิดพร้อม Windows" ได้ในหน้าติดตั้ง | ไม่มีระบบอัปเดต ต้องทำเอง |
| MSIX | ติดตั้ง/ถอนสะอาด | ต้องเซ็นโปรแกรมก่อนถึงจะติดตั้งได้ และ registry ถูกแยกไว้ ทำให้การเปิดพร้อม Windows ต้องใช้วิธีเฉพาะ |

**เลือก Velopack** (ใช้เองและแจกคนอื่น หัวข้อ 8 ข้อ 4): app ที่เปิดค้างไว้ทั้งวันควรอัปเดตตัวเองได้ ไม่อย่างนั้นผู้ใช้จะค้างอยู่ที่เวอร์ชันเก่า

**ข้อที่ปรับจากความต้องการ**
- **ติดตั้งแบบรายผู้ใช้:** ลงที่ `%LocalAppData%\MinkQuickLax` ไม่ต้องใช้สิทธิ์ admin และไม่มีหน้าต่าง UAC เด้งขึ้นมา
- **รวม .NET ไว้ในตัว (self-contained):** ผู้ใช้ไม่ต้องลง .NET เอง แต่ตัวติดตั้งจะใหญ่ขึ้นเป็นหลักสิบ MB (ต้องวัดจริง)
- **Shortcut:** สร้างใน Start Menu เสมอ ส่วนบน Desktop ไม่สร้างให้ เพราะไอคอนลอยของ app ก็อยู่บนจออยู่แล้ว
- **ถอนการติดตั้ง:** ลบรายการเปิดพร้อม Windows ออกทุกครั้ง และถามก่อนว่าจะลบค่าตั้งใน `%AppData%\MinkQuickLax\` ด้วยไหม

**การเซ็นโปรแกรม (ถ้าจะแจกคนอื่น)**
- **ถ้าไม่เซ็น:** เปิดตัวติดตั้งแล้วจะขึ้น "Windows protected your PC" ต้องกด More info → Run anyway
- **เซ็นแล้วก็ยังเตือนช่วงแรก:** ตั้งแต่ปี 2024 ใบรับรองแบบ EV ก็ไม่ข้ามคำเตือนทันทีแล้ว ทั้ง OV และ EV ต้องมีคนดาวน์โหลดสะสมไปสักพักก่อน
- **Azure Artifact Signing** (ชื่อเดิม Trusted Signing) ราคาถูกที่สุด แต่แบบบุคคลเปิดให้เฉพาะคนในสหรัฐฯ และแคนาดา คนในไทยจึงต้องซื้อใบรับรอง OV จากผู้ออกใบรับรองแทน
- **สรุป:** ช่วงแรกยังไม่เซ็น ใส่วิธีกด Run anyway ไว้ในคู่มือและหน้าดาวน์โหลด ค่อยซื้อใบรับรอง OV ตอนมีผู้ใช้มากขึ้น

### แจกให้คนอื่น
- **ระบบอัปเดตต้องมีตั้งแต่เวอร์ชันแรกที่แจก**
  - ถ้าเวอร์ชันแรกไม่มี คนที่ติดตั้งไปแล้วจะไม่มีวันได้รับอัปเดต ต้องไปดาวน์โหลดใหม่เอง
  - → ย้ายจากเฟส 2 เข้าเฟส 1
- **แยกช่องทางอัปเดตเป็นทดลอง (beta) กับปกติ (stable)**
  - เครื่องของเราเองรับ beta ใช้จริงสักพักก่อน แล้วค่อยปล่อยเป็น stable ให้คนอื่น
  - Velopack รองรับการแยกช่องทางอยู่แล้ว
- **ที่วางไฟล์ติดตั้งและไฟล์อัปเดต:** รอคำตอบหัวข้อ 8 ข้อ 9
- **ทดสอบบนเครื่องใหม่ทุกครั้งก่อนแจก**
  - ใช้ Windows Sandbox (มีใน Windows 11 Pro) ซึ่งเป็นเครื่องที่ไม่มี .NET และไม่มีค่าตั้งเดิมค้างอยู่
  - ลองติดตั้ง อัปเดตจากเวอร์ชันก่อน และถอนการติดตั้ง
- **หน้าเกี่ยวกับ:** แสดงสัญญาอนุญาตของส่วนประกอบที่นำมาใช้ เช่น ฟอนต์ (OFL), WPF-UI และ Velopack (MIT)
- **ความเป็นส่วนตัว**
  - app ไม่ส่งข้อมูลผู้ใช้ออกไปไหน มีแค่ตรวจหาอัปเดตกับดึง favicon จากเว็บที่ผู้ใช้ลงทะเบียนไว้
  - เขียนบอกไว้ในคู่มือ และปิดการตรวจหาอัปเดตได้

### เปิดพร้อม Windows

**ข้อที่ปรับจากความต้องการ**
- **"ตอนเปิดเครื่อง" ที่ทำได้จริงคือ "ตอนเข้าสู่ระบบ (sign in)"**
  - app ที่มีหน้าตาต้องรอให้ผู้ใช้เข้า Windows ก่อนถึงจะแสดงผลได้
  - ไม่ควรทำเป็น Windows Service
- **ตัว app ต้องไม่รันเป็น admin**
  - app ทุกตัวที่กดเปิดจากไอคอนจะได้สิทธิ์ admin ตามไปด้วย ซึ่งอันตราย
  - ลากไฟล์จาก File Explorer มาวางจะไม่ได้ เพราะ Windows ไม่ให้ลากจากโปรแกรมสิทธิ์ต่ำไปวางในโปรแกรมสิทธิ์สูง
  - ถ้าบาง app ต้องเปิดเป็น admin ให้ตั้งเป็นราย link แทน (หัวข้อ 4.4)

**วิธีทำ**
- **ลงทะเบียน:** เขียนค่าไว้ที่ `HKCU\Software\Microsoft\Windows\CurrentVersion\Run` ไม่ต้องใช้สิทธิ์ admin
- **สวิตช์:** อยู่ในหน้าตั้งค่าหมวดทั่วไป ค่าเริ่มต้นเป็นเปิด
- **ให้ตรงกับ Task Manager**
  - ถ้าผู้ใช้ปิดใน Task Manager → Startup apps ไปแล้ว (Windows เก็บไว้ที่ `...\Explorer\StartupApproved\Run`) หน้าตั้งค่าต้องแสดงว่าปิด
  - ห้ามแอบเปิดกลับเองเงียบ ๆ
- **เปิดแบบเงียบ:** ตอนเปิดพร้อม Windows ให้แสดงแค่ไอคอนกับ tray ไม่เปิดหน้าตั้งค่า (ส่ง argument `--startup`)
- **กรณีจอยังไม่พร้อมตอนเข้า Windows และเปิดซ้ำ:** ดูตารางในหัวข้อ 3

---

## 12. ไอคอนเดี่ยว กลุ่ม และการลากวาง

### ข้อที่ปรับจากความต้องการ
- **ไม่ต้องมีสวิตช์เลือกโหมด "เดี่ยว / กลุ่ม / ทั้งคู่"** (ยืนยันแล้ว หัวข้อ 8 ข้อ 6)
  - ถ้ารองรับทั้งคู่อยู่แล้ว "เดี่ยวอย่างเดียว" ก็คือไม่สร้างกลุ่ม ส่วน "กลุ่มอย่างเดียว" ก็คือไม่มีไอคอนเดี่ยว
  - ถ้ามีสวิตช์ จะเกิดคำถามที่ไม่มีคำตอบดี ๆ เช่น สลับจาก "ทั้งคู่" ไปเป็น "เดี่ยว" แล้วกลุ่มที่มีอยู่จะไปอยู่ไหน
  - **แทนด้วย:** ของบนจอแต่ละชิ้นเป็นไอคอนเดี่ยวหรือกลุ่มก็ได้ มีกี่ชิ้นก็ได้ และปนกันได้
  - ใครอยากเริ่มแบบไหน เลือกได้ในหน้าต้อนรับครั้งแรก ซึ่งใช้แค่จัดวางตั้งต้น (หัวข้อ 15)
- **ปุ่มเดี่ยวมากกว่าหนึ่ง:** แบบไอคอนเดี่ยวรองรับอยู่แล้ว (หัวข้อ 10) และกลุ่มก็มีได้หลายกลุ่มเหมือนกัน
- **ไม่ควรมีกลุ่มซ้อนในกลุ่ม:** ต้องกดหลายชั้นกว่าจะถึง link ซึ่งขัดกับจุดประสงค์ของ app ที่ต้องการให้เปิดได้เร็ว

### แยก "link" กับ "ของที่วางบนจอ" ออกจากกัน (ยืนยันแล้ว หัวข้อ 8 ข้อ 7)
- **link** คือข้อมูล: ชื่อ, icon, เป้าหมาย, argument
- **ของบนจอ** คือไอคอนเดี่ยวหรือกลุ่ม ที่ชี้ไปหา link
- **ข้อดี**
  - link ตัวเดียวอยู่ได้หลายที่ เช่น Chrome เป็นไอคอนเดี่ยว และอยู่ในกลุ่ม "งาน" ด้วย
  - แก้ชื่อหรือ icon ครั้งเดียว เปลี่ยนทุกที่
- **วาง link ที่มีอยู่แล้วเพิ่มอีกที่**
  - คลิกขวาที่ไอคอน → "วางเพิ่มบนจอ" หรือ "เพิ่มเข้ากลุ่ม…"
  - ในหน้าตั้งค่าหมวด Link ลากจากรายการไปวางบนจอได้
- **ต้องแยกคำสั่งให้ชัด**
  - **เอาออกจากจอ:** link ยังอยู่ในรายการของหน้าตั้งค่า
  - **ลบ link:** หายไปจากทุกที่ ต้องถามยืนยันก่อน และบอกด้วยว่าวางอยู่กี่ที่
- **หน้าตั้งค่ากรองได้:** "ยังไม่ได้วางไว้ที่ไหน" ใช้หา link ที่เอาออกจากจอไปหมดแล้ว

### หน้าตากลุ่ม

| แบบ | ตอนปกติ | ตอนใช้ | เหมาะกับ |
|---|---|---|---|
| **โฟลเดอร์** ⭐ ค่าเริ่มต้น | ไอคอนเดียว ขนาดเท่าไอคอนเดี่ยว ข้างในเป็นไอคอนย่อ 2×2 | คลิกแล้วกางออกเป็นแผง | link เยอะแต่ไม่ได้ใช้ทุกตัวบ่อย |
| **แถบ** (เฟส 2) | ไอคอนเรียงเป็นแถวแนวนอนหรือแนวตั้ง มีชื่อกลุ่ม | กดได้เลย ไม่ต้องกาง | ใช้บ่อย อยากกดครั้งเดียวจบ |

- **เปิดโฟลเดอร์ด้วยการคลิกเป็นค่าเริ่มต้น:** ส่วนการชี้แล้วกางให้เป็นตัวเลือก เพราะมีปัญหาเมาส์แค่ผ่านแต่แผงกางเอง (หัวข้อ 3)
- **ทิศที่กาง:** เลือกให้เองตามที่ว่าง เช่นโฟลเดอร์ชิดขอบขวาของจอให้กางไปทางซ้าย
- **แผงที่กาง:** หุบเมื่อกด link, กด Esc หรือคลิกที่อื่น · ใช้หน้าตาแผงจาก mockup v0.1 และใช้ Acrylic ได้ (หัวข้อ 9)
- **ค่าตั้งของแต่ละกลุ่ม:** ชื่อ, icon ของโฟลเดอร์ (ไอคอนย่อ 2×2 หรือตั้งเอง), จำนวนคอลัมน์, แสดงชื่อ link

### การลากวาง

**ข้อที่ปรับจากความต้องการ**
- **"ตรงไหนก็ได้" ควรมีข้อยกเว้นเดียว คือค่าเริ่มต้นห้ามวางทับ taskbar**
  - taskbar ก็ลอยบนสุดเหมือนกัน ถ้าวางทับจะแย่งกันขึ้นบน และไอคอนจะหายไปข้างหลังทุกครั้งที่กด taskbar
  - เปิดให้วางทับได้ในค่าตั้งขั้นสูง
- **ลากข้ามจอได้:** ส่วนกรณีถอดจอออก ดูตารางในหัวข้อ 3
- **กันลากโดยไม่ตั้งใจ แต่ไม่ให้ยุ่งยาก**
  - โหมดใช้งานยังล็อกตำแหน่งไว้เหมือนเดิม
  - เพิ่มทางลัด **Ctrl+ลาก** ย้ายทีละตัวได้ทันที ไม่ต้องเข้าโหมดแก้ไข
  - โหมดแก้ไขเก็บไว้ใช้ตอนจัดทีละหลายตัว

**สิ่งที่ลากได้ในโหมดแก้ไข**

| ลากอะไร | ไปวางที่ไหน | ผลที่ได้ |
|---|---|---|
| ไอคอนเดี่ยว | ที่ว่างบนจอ | ย้ายตำแหน่ง |
| ไอคอนเดี่ยว | บนกลุ่ม | เข้ากลุ่ม |
| ไอคอนเดี่ยว | ค้างไว้บนไอคอนเดี่ยวอีกตัว 0.6 วินาที | สร้างกลุ่มใหม่จากสองตัวนั้น (แบบหน้าจอมือถือ) |
| link ในแผงกลุ่ม | ออกนอกแผง | กลายเป็นไอคอนเดี่ยว |
| link ในแผงกลุ่ม | ตำแหน่งอื่นในแผง | จัดลำดับใหม่ |
| กลุ่มทั้งกลุ่ม | ที่ว่างบนจอ | ย้ายทั้งกลุ่ม |
| ไฟล์, shortcut หรือ URL จากนอก app | ที่ว่าง หรือบนกลุ่ม | ลงทะเบียนเป็น link ใหม่ (หัวข้อ 4.3 เฟส 2) |

---

## 13. หน้าตั้งค่า

### ข้อที่ปรับจากความต้องการ
- **"ครบถ้วน" ต้องไม่ใช่ "ทุกอย่างกองอยู่หน้าเดียว"**
  - ของที่ปรับบ่อยอยู่บนสุดของแต่ละหมวด ของที่ไม่ค่อยได้แตะให้พับไว้ใต้ "ขั้นสูง"
  - มีช่องค้นหาค่าตั้งอยู่ด้านบน
- **ปรับแล้วมีผลทันที ไม่มีปุ่มบันทึก** แบบหน้า Settings ของ Windows 11 เห็นผลบนไอคอนจริงทันที
  - แต่ละหมวดมีปุ่ม "คืนค่าเริ่มต้น"
- **ค่าตั้งแต่ละข้อมีปุ่ม ⓘ** กดแล้วเปิดคู่มือตรงหัวข้อนั้น (หัวข้อ 14)
- **หน้าตา:** ใช้แบบมาตรฐาน Windows 11 (library WPF-UI) แล้วแต่งสีตามสไตล์ที่เลือก เพราะหน้าตั้งค่าต้องอ่านง่ายก่อนสวย
- **เป็นหน้าต่างปกติ:** โผล่ใน taskbar และ Alt+Tab ได้ ต่างจากไอคอนลอยที่ซ่อนไว้

### หมวด (ร่าง)

| หมวด | มีอะไรบ้าง |
|---|---|
| **Link** | รายการ link ทั้งหมด ค้นหาและกรองตามประเภทได้ · เพิ่ม, สแกนหา app, แก้ไข, ลบ · ชื่อ, icon, เป้าหมาย, argument, working directory, run as admin · ดูว่า link นี้วางอยู่ที่ไหนบ้าง |
| **กลุ่ม** | สร้าง/ลบกลุ่ม · แบบโฟลเดอร์หรือแถบ · จำนวนคอลัมน์ · ลำดับ link ในกลุ่ม |
| **หน้าตา** | สไตล์ HUD/Dot Matrix (สลับได้ทันที หัวข้อ 9) · ธีมสว่าง/มืด/ตาม Windows · ขนาดไอคอน 32–64px · ความทึบตอนไม่ได้ใช้ · ระยะที่เมาส์เข้าใกล้แล้วไอคอนชัดขึ้น · แสดงชื่อใต้ไอคอน · ลดภาพเคลื่อนไหว (ค่าเริ่มต้นตามค่า Animation effects ของ Windows) |
| **ซ่อน/แสดง** | คลิก icon ที่ tray เพื่อซ่อน/แสดงทั้งชุด (ค่าเริ่มต้นเปิด) · ซ่อนเองตอนเปิด app เต็มจอหรือกำลัง present งาน (ค่าเริ่มต้นเปิด) · คีย์ลัดซ่อน/แสดงทั้งชุด (ตั้งได้ในหมวดคีย์ลัด) |
| **การใช้งาน** | คลิกเดียวหรือดับเบิลคลิกเพื่อเปิด · สลับไปหน้าต่างเดิมถ้า app เปิดอยู่ · เวลาชี้ค้างก่อนชื่อขึ้น · เปิดกลุ่มด้วยการคลิกหรือชี้ |
| **ตำแหน่ง** | ขนาดกริด · ชิดกริด/ชิดแนว · วางทับ taskbar ได้ไหม · เปิด/ปิด Ctrl+ลาก · จัดเรียงใหม่ · คืนตำแหน่งทั้งหมด |
| **คีย์ลัด** | คีย์เรียก launcher (ค่าเริ่มต้น `Win+Alt+Q`) · คีย์ซ่อน/แสดงทั้งชุด (ค่าเริ่มต้นไม่ได้ตั้ง) · ปิดคีย์ลัด · เตือนเมื่อคีย์ชน (หัวข้อ 4.1) |
| **ทั่วไป** | เปิดพร้อม Windows · ภาษาไทย/English (ค่าเริ่มต้นตามภาษาของ Windows) · ตรวจหาอัปเดต · ช่องทางอัปเดตแบบปกติ/ทดลอง (หัวข้อ 11) |
| **ข้อมูล** | สำรอง/นำเข้า/ส่งออกค่าตั้ง · กู้คืนจากไฟล์สำรองอัตโนมัติ · เปิดโฟลเดอร์ค่าตั้ง · คืนค่าทั้งหมด |
| **เกี่ยวกับ** | เวอร์ชัน · เปิดคู่มือ · สัญญาอนุญาตของส่วนประกอบที่นำมาใช้ · ปุ่มคัดลอกข้อมูลสำหรับแจ้งปัญหา (เวอร์ชัน Windows, จอ, DPI, log ล่าสุด) |

### เรื่องไฟล์ค่าตั้ง
- **ใส่เลขเวอร์ชันไว้ในไฟล์ (`schemaVersion`):** อัปเดต app แล้วแปลงค่าตั้งเก่าให้อัตโนมัติ
- **สำรองอัตโนมัติ:** เก็บย้อนหลัง 10 ชุด กู้คืนได้จากหมวดข้อมูล

---

## 14. คู่มือการใช้งาน (HTML)

### ข้อที่ปรับจากความต้องการ
- **ต้องใช้ได้แบบไม่มีอินเทอร์เน็ต**
  - ไฟล์คู่มือติดมากับตัวติดตั้ง
  - ฟอนต์ รูป และ script รวมอยู่ในไฟล์ ไม่โหลดจาก CDN หรือ Google Fonts
- **ค้นหาภาษาไทยต้องทำเอง ใช้ library ค้นหาทั่วไปไม่ได้**
  - ภาษาไทยไม่เว้นวรรคระหว่างคำ library ที่ตัดคำด้วยช่องว่าง (เช่น Lunr) จะหาคำที่อยู่กลางประโยคไม่เจอ
  - ใช้การหาข้อความย่อย (substring) เป็นหลัก แล้วใช้ `Intl.Segmenter('th')` ตัดคำไว้สำหรับไฮไลต์และจัดอันดับผล
  - แต่ละหัวข้อมีคำค้นเพิ่มทั้งไทยและอังกฤษ เช่น พิมพ์ "hotkey" แล้วเจอหัวข้อ "คีย์ลัด"
- **ถ้าเปิดผ่าน browser ตามปกติ จะเปิดไปตรงหัวข้อไม่ได้**
  - Windows ตัด `#หัวข้อ` ท้ายที่อยู่ไฟล์ `file:///` ทิ้งตอนส่งต่อให้ browser ปุ่ม ⓘ ในหน้าตั้งค่าจึงพาไปตรงหัวข้อไม่ได้
  - **ทางที่เสนอ:** เปิดคู่มือในหน้าต่างของ app เองด้วย WebView2 (มีมากับ Windows 11 อยู่แล้ว)
    - ไปตรงหัวข้อได้ และธีมตรงกับ app
    - ใช้ RAM เฉพาะตอนเปิดคู่มืออยู่
    - มีปุ่ม "เปิดใน browser" และ "พิมพ์" ให้ด้วย

### หน้าตาและการใช้งาน
- **สไตล์เดียวกับ app** (HUD หรือ Dot Matrix) รองรับธีมสว่างและมืด
- **สารบัญด้านซ้าย:** ไฮไลต์หัวข้อที่กำลังอ่านอยู่ · ถ้าหน้าต่างแคบให้พับเป็นเมนู
- **ค้นหา**
  - กด `/` หรือ `Ctrl+K` เพื่อเริ่มค้นหา
  - ผลขึ้นทันทีระหว่างพิมพ์ พร้อมข้อความรอบ ๆ คำที่เจอ
  - กด ↑↓ เลือกผล กด Enter ไปที่หัวข้อนั้นและไฮไลต์คำที่ค้น
- **เปิดคู่มือ:** กด F1 ในหน้าตั้งค่า, เมนูที่ tray, หรือปุ่ม ⓘ
- **ภาพหน้าจอ:** ใส่เท่าที่จำเป็น เพราะต้องถ่ายใหม่ทุกครั้งที่ UI เปลี่ยน · ใช้ภาพเคลื่อนไหวสั้น ๆ กับเรื่องการลากวาง

### เนื้อหา (ร่าง)
1. **เริ่มต้นใช้งาน:** ติดตั้ง, หน้าเตือน SmartScreen, หน้าต้อนรับ
2. **เพิ่ม link:** สแกนหา app, ลากไฟล์มาวาง, URL, คำสั่ง
3. **ไอคอนเดี่ยวและกลุ่ม**
4. **จัดวาง:** โหมดแก้ไข, Ctrl+ลาก, ใช้หลายจอ
5. **คีย์ลัดและการค้นหา**
6. **ค่าตั้งทุกข้อ:** ตรงกับปุ่ม ⓘ ในหน้าตั้งค่า
7. **แก้ปัญหา:** ไอคอนหาย, คีย์ลัดไม่ทำงาน, ไอคอนโดนบัง, ไม่เปิดพร้อม Windows
8. **สำรองข้อมูล ย้ายเครื่อง ถอนการติดตั้ง**
9. **คำถามที่พบบ่อย**

### สองภาษา (ไทยและอังกฤษ)
- **เปิดตามภาษาที่ตั้งไว้ใน app** และมีปุ่มสลับภาษาในหน้าคู่มือ
- **ทั้งสองภาษาใช้ id หัวข้อชุดเดียวกัน** (ตั้งชื่อเป็นภาษาอังกฤษ เช่น `settings-idle-opacity`)
  - ปุ่ม ⓘ พาไปหัวข้อได้ถูกทั้งสองภาษา
  - กดสลับภาษาแล้วยังอยู่หัวข้อเดิม
- **ค้นหาได้ทั้งสองภาษาในทุกหน้า:** คำค้นเพิ่มของแต่ละหัวข้อมีทั้งไทยและอังกฤษอยู่แล้ว
- **เนื้อหาภาษาไทยทำก่อน** ส่วนภาษาอังกฤษทำในเฟส 2 ระหว่างนั้นหัวข้อที่ยังไม่แปลให้แสดงภาษาไทยพร้อมป้ายบอกว่ายังไม่มีภาษาอังกฤษ

### วิธีทำ
- **เขียนเนื้อหาเป็น Markdown** แยกโฟลเดอร์ตามภาษา (`manual/th/`, `manual/en/`) แล้วใช้ script ตอน build แปลงเป็น HTML ภาษาละไฟล์ พร้อมดัชนีสำหรับค้นหา แก้เนื้อหาง่ายกว่าไปแก้ HTML ตรง ๆ
- **มี test ตรวจตอน build**
  - ปุ่ม ⓘ ทุกตัวต้องมีหัวข้ออยู่จริง
  - หัวข้อต้องมีครบทั้งสองภาษา ถ้าขาดให้ขึ้นเตือน

---

## 15. ข้อเสนอเพิ่มเติมรอบ 2

### หน้าต้อนรับครั้งแรก (เฟส 2)
1. เลือกภาษาและสไตล์
2. สแกนหา app แล้วติ๊กเลือกตัวที่ใช้บ่อย
3. เลือกจัดวางตั้งต้น: เป็นไอคอนเดี่ยวทั้งหมด หรือรวมไว้ในกลุ่มเดียว
4. เปิดพร้อม Windows และตั้งคีย์ลัด
5. สอนสั้น ๆ: Ctrl+ลาก, คลิกขวา, icon ที่ tray (กดข้ามได้)

- ใช้แทนสวิตช์โหมดในหัวข้อ 12 และแทนหน้าวิซาร์ดที่ Velopack ไม่มี (หัวข้อ 11)
- เฟส 1 ยังไม่มีหน้านี้ ตอนเปิดครั้งแรกให้ขึ้นหน้าสแกนหา app แทน

### เรื่องที่ควรวางไว้ตั้งแต่เฟส 1 เพราะแก้ทีหลังยาก
- **ข้อความใน app เก็บเป็นไฟล์แปล (`.resx`) ไทยและอังกฤษ** (ยืนยันแล้ว หัวข้อ 8 ข้อ 8)
  - ไม่เขียนข้อความลงในโค้ดตรง ๆ
  - มี test ตรวจว่าทุกข้อความมีครบทั้งสองภาษา
- **ออกแบบ UI ให้รองรับทั้งสองภาษา**
  - ภาษาไทยมีสระบน สระล่าง และวรรณยุกต์ ต้องเผื่อความสูงบรรทัด ไม่อย่างนั้นจะโดนตัดขอบ
  - ภาษาอังกฤษมักยาวกว่า ปุ่มและป้ายชื่อไม่ควรกำหนดความกว้างตายตัว
  - ทดสอบทุกหน้าจอทั้งสองภาษา
- **log:** เก็บย้อนหลัง 7 วันที่ `%LocalAppData%\MinkQuickLax\logs` ถ้า app มีปัญหาบนเครื่องคนอื่น จะได้มีข้อมูลไว้ดู
- **อัปเดตอัตโนมัติ:** ตรวจตอนเปิด app แจ้งเตือนที่ tray แล้วติดตั้งตอนปิด app ไม่ขัดจังหวะงาน (ต้องมีตั้งแต่เวอร์ชันแรกที่แจก หัวข้อ 11)

---

## 16. ขั้นตอนถัดไป

> **ย้ายไปแล้ว:** ลำดับงานต่อจากนี้อยู่ที่ [docs/PLAN.md](docs/PLAN.md) รายการด้านล่างเก็บไว้เป็นประวัติ

1. ~~เลือกสไตล์~~ → Glass เป็นค่าเริ่มต้น HUD และ Dot Matrix เป็นตัวเลือก (หัวข้อ 8 ข้อ 10)
2. ~~ตอบคำถามที่เหลือในหัวข้อ 8~~
3. ~~ทำ mockup หน้าตา: ตอนหุบ, ตอนกาง, หน้าสแกน app~~ (v0.1 ดูหัวข้อ 9)
4. ~~ทำ mockup แบบไอคอนเดี่ยว~~ (v0.2 ดูหัวข้อ 10)
5. ~~ทำ mockup สไตล์ technology~~ (v0.3 ดูหัวข้อ 9)
6. ~~ตรวจความต้องการรอบ 2~~ (หัวข้อ 11–15)
7. ทำ mockup v0.4
   - กลุ่มแบบโฟลเดอร์ และการลากเข้า/ออก
   - หน้าตั้งค่า: สลับสไตล์, ซ่อน/แสดง, สลับภาษาไทย/English
   - หน้าคู่มือพร้อมช่องค้นหาและปุ่มสลับภาษา
8. ~~ทดสอบหน้าต่างโปร่งใส 20–30 บานบนเครื่องจริง~~ → docs/PLAN.md M1 (S1)
9. ~~สรุปเครื่องมือที่ใช้~~ → docs/PLAN.md หัวข้อ 1 · เริ่มเฟส 1 ที่ M0
