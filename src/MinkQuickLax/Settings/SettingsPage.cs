namespace MinkQuickLax.Settings;

/// <summary>The sections of the settings window (SPEC 4.8), in menu order.</summary>
public enum SettingsPage
{
    Links,
    Groups,
    Appearance,
    Visibility,
    Behavior,
    Position,
    General,
    Data,
    About,
}
